# Expand the same Whiteboard in Focus mode

A Whiteboard may expand from the Right sidebar into the main working area and later return without creating a second document or losing its viewport, zoom, tab position, or Project identity.

## Consequences

Focus mode is a presentation state of the existing Whiteboard tool, not a separate route or separately owned workspace.
