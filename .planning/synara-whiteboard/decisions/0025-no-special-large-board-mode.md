# Do not add a special large-Whiteboard mode

The feature will not impose a product-level element limit, display a dedicated large-board warning, or switch into a reduced-function mode based on board size. Normal performance engineering and diagnostics still apply, but large-board UX is not a separate product capability.
