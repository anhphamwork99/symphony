# Remove stale unsent Whiteboard chips

If a selected element or its Whiteboard is deleted before the message is sent, Synara removes the corresponding unsent chip. If the source is temporarily unavailable rather than deleted, the chip remains with an explicit diagnostic and contributes no empty or fabricated context. Previously sent element snapshots remain unchanged.
