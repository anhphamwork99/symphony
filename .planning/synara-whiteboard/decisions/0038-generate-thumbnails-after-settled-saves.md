# Generate thumbnails after settled saves

Launcher thumbnails are generated only after a native Whiteboard has auto-saved and remained unchanged for an idle interval. Synara caches a small preview, never regenerates it per pointer movement or streamed agent update, and displays a Whiteboard icon when no cached thumbnail exists.
