# Duplicate complete Whiteboard content

Duplicate creates a new Whiteboard identity containing copies of all elements and referenced image assets. It uses `<original name> copy`, then `copy 2`, `copy 3`, resets Undo/Redo, opens as the active tab, and does not copy an active AI operation or Whiteboard selection chips from the composer.
