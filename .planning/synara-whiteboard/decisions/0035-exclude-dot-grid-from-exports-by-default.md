# Exclude the dot grid from exports by default

PNG and SVG exports omit the canvas dot grid unless the user enables `Include grid background`. The `.excalidraw` representation preserves the grid display setting, and the grid remains a rendering preference rather than document elements.
