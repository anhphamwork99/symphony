# Provide a Whiteboard launcher

The Right sidebar will offer a Whiteboard launcher with New Whiteboard, recent boards with thumbnails and update times, name search, and per-board Rename, Duplicate, Export, and Delete actions. Opening an already-open Whiteboard activates its existing tab instead of creating a duplicate tab.
