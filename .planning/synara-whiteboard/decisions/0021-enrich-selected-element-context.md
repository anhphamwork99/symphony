# Enrich selected element context without adding chips

Each visible Whiteboard selection chip represents exactly one selected element. At send time Synara may add the minimal directly related context needed to preserve meaning, such as bound text, connected arrow endpoints, or group and frame relationships. Related context is marked separately and does not create additional chips or imply that the user selected those elements.
