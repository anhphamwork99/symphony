# Name new Whiteboards board

The first unnamed Whiteboard uses `board`. Later automatic names use the next available numbered form: `board 2`, `board 3`, and so on. Naming remains editable and stable Whiteboard identity does not depend on the display name.
