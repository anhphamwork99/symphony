# Create Whiteboards explicitly and share them across Project conversations

Synara will create a Whiteboard only after an explicit user action rather than provisioning an empty one for every Project. Every Main conversation in the Project can access it, while deleting the whole Whiteboard always requires confirmation.

## Consequences

Empty Projects carry no unused Whiteboard data, and a Whiteboard never becomes stranded behind the conversation from which it was created.
