# Stop on an invalid agent operation

If a streamed agent operation fails validation, Synara does not apply it or continue building later operations on the missing result. The AI edit stops, prior valid partial updates remain visible, the Whiteboard unlocks, the status surface explains the failure and offers Retry, and one Undo removes the complete partial AI edit batch.
