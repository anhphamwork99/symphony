WP-02 renewed provenance — HISTORICAL supporting record and Decision 0008 reset
====================================================================================
Recorded: 2026-08-28 (local, UTC+7)
This artifact preserves the renewed attempt provenance at historical candidate
ffd45bd. It is not current WP-02 proof. The integrated leg passed, the
canonical-identity leg failed, lifecycle/restart/resume were not run, and the
attempt's no-retry state is spent. The raw logs are immutable.

BINDING RESET
-------------
Decision 0008 is aspect-scoped Authoritative for the post-await live-control
retirement classification only. Before any new D/R evidence, implement and
freeze an exact two-file candidate child of ffd:
  apps/server/src/provider/piSubagentLiveLifecycleContainment.ts
  apps/server/src/provider/piSubagentLiveLifecycleContainment.test.ts
The new candidate SHA is unknown until implementation. Its delta from ffd must
be exactly those two files; its total delta from 12fd6686 must be exactly four
files including the two Decision 0007 fixture files. No canonical expectation,
Alfie source/pin, third file, coordinator/configuration, or unrelated path may
change. Alfie remains 3fe340b401ca86bcbe8b55abd4de107e1d93482e /
@alfie/pi-subagents@0.15.0-alfie.6.

EVIDENCE RESET ROUTE
--------------------
containment two-file child of ffd -> freeze -> same WP-01 closed set (296
baseline plus exact focused cases added by implementation; record expected count
only after implementation) -> exactly one new full five-file WP-02 -> fresh
owner WP-03 -> fresh owner WP-04 -> WP-05 -> WP-06 Decision 0009 -> closure.
Historical ffd/WP-01/current renewed WP-02 evidence is supporting only.
Old WP-03/WP-04 authorization is non-transferable; no retry is authorized.
No producer, test, source implementation, or quality command is run by this
planning reset. Protected owner WIP remains outside the transaction and must
remain unstaged with required aggregate hash:
  ab8f8f54fe818819721f737aa337156ed6348c7410c55083ce3a67785bb7eaa8

PRESERVED HISTORICAL PROVENANCE APPENDIX
-----------------------------------------
WP-02 non-destructive real-Pi provenance and zero-delta gate record — Ticket 06
===============================================================================
Recorded: 2026-08-28 (local, UTC+7) · host: Darwin arm64 (macOS)
Executor: WP-02 implementation worker, evidence-only packet
Class of this record: P (planning/provenance). The five producers below are R
class (controlled real-Pi, non-destructive).
Consumes: WP-01 evidence (commit 9208e1728) and the WP-01-retained worktrees.
This record makes NO destructive process claim of any kind. The manual
destructive leg (SYNARA_T17_MANUAL_TEARDOWN=1) is excluded from every producer
by explicit env removal (`env -u SYNARA_T17_MANUAL_TEARDOWN`).

1. FROZEN COMPOSITION (re-verified at WP-02 start)
--------------------------------------------------
Symphony behavioral candidate (detached):
  path    : /tmp/symphony-t06
  HEAD    : 12fd6686edc26a3fa0382e8bdeb83a1be8045539
            == WP-01 pin, exact
  branch  : `git status -b --short` → "## HEAD (no branch)" (detached)
  tracked delta (status --short --untracked-files=no): 0 lines
  Pi acceptance-surface working-tree diff (7 named paths): EMPTY

Alfie controlled provider worktree (detached):
  path    : /tmp/alfie-t06
  HEAD    : 3fe340b401ca86bcbe8b55abd4de107e1d93482e
            == WP-01 pin and fixture pinnedCommit, exact
  branch  : "## HEAD (no branch)" (detached)
  status  : 0 lines (clean)
  origin  : https://github.com/anhphamwork99/alfie.git
            == fixture expectedRepositoryUrl (exact match)

User Alfie checkout: /Users/anhpham99/alfie (UNTOUCHED — not referenced by any
producer; ALFIE_REPO_DIR points exclusively at /tmp/alfie-t06).

2. PROVENANCE FIXTURE (5/5 hashes re-verified at WP-02 start)
-------------------------------------------------------------
Fixture: apps/server/src/provider/test-fixtures/piSubagentExtensionProvenance.json
  expectedRepositoryUrl : https://github.com/anhphamwork99/alfie.git   MATCH
  pinnedCommit          : 3fe340b401ca86bcbe8b55abd4de107e1d93482e     MATCH
  packageName           : @alfie/pi-subagents      (package.json: MATCH)
  packageVersion        : 0.15.0-alfie.6           (package.json: MATCH)
shasum -a 256 recomputed from /tmp/alfie-t06 at WP-02 start — ALL MATCH:
  954c5297446149d2e6a997c4f0eefac768611f92599cecab2a5f96255dafa22f
    agent/extensions/pi-subagents/package.json
  c843406cf94bffdfa7304e2c412766b7cbbcdbfc17fa2846046c77992288d1f9
    agent/extensions/pi-subagents/src/index.ts
  1f99fd0794c8c32a2229b4f7bc7f3da221ce50d69b221875ba7e8f0f8d879783
    agent/extensions/pi-subagents/src/agent-manager.ts
  98a4c592b14bd7b66b42ea26aabf337d01b6146e618e2c2d67852449a755b1d2
    agent/extensions/pi-subagents/src/agent-runner.ts
  ef44dc6d91ba400187967568b18483792eef3715c616b56aea24e3bb0c48f3c0
    agent/extensions/pi-subagents/src/child-bash-supervisor.ts

3. TOOLCHAIN AND PI SDK
-----------------------
node  v24.14.1   (>= 24.10 required by piSubagentRealPiAcceptance.test.ts's
                  node:sqlite import; producer 1 runs under Node)
bun   1.3.12     (producers 2–5 run under the repository-pinned Bun runtime)
vitest 4.1.10    (repository-pinned; `RUN v4.1.10` banner in each log)
Pi SDK (installed under /tmp/symphony-t06/node_modules/.bun, resolved by
bun install --frozen-lockfile at WP-01, manifests read at WP-02):
  @earendil-works/pi-agent-core     0.83.0
  @earendil-works/pi-ai             0.83.0
  @earendil-works/pi-coding-agent   0.83.0
  @earendil-works/pi-tui            0.83.0
  (all four == ^0.83.0 declared in apps/server/package.json)

4. PROTECTED OWNER WIP GATE (main checkout /Users/anhpham99/symphony)
---------------------------------------------------------------------
Main HEAD at WP-02 start: 9208e17288a4dd0862c8078f4ecdd332fff02652
  (WP-01 evidence commit).
Pre-run aggregate WIP diff hash:
  git diff -- apps/web/package.json apps/web/src/main.tsx bun.lock | shasum -a 256
  → ab8f8f54fe818819721f737aa337156ed6348c7410c55083ce3a67785bb7eaa8  EXACT
Staged files at WP-02 start: 0 (`git diff --cached --name-only` empty).
Pi-acceptance-surface zero-delta gate, pre-run:
  git diff --name-status 12fd6686edc26a3fa0382e8bdeb83a1be8045539..HEAD -- \
    apps/server/src/provider apps/server/src/persistence \
    apps/server/src/orchestration apps/server/scripts/wallclock-tests.ts \
    apps/server/vitest.config.ts packages/contracts/src/piSubagents.ts
  → EMPTY.
Main status --short: exactly the three protected files modified-unstaged.

5. PRODUCER AUTHORIZATION AND DESTRUCTIVE-LEG EXCLUSION
-------------------------------------------------------
Exactly five standalone wallclock files are authorized (WP-02 §"The five
standalone files"); each runs in its OWN `vitest run --project wallclock
--maxWorkers=1 --no-file-parallelism` process, serially, from
/tmp/symphony-t06/apps/server, with ALFIE_REPO_DIR=/tmp/alfie-t06 and
`env -u SYNARA_T17_MANUAL_TEARDOWN` so the manual destructive test
("MANUAL T17-AC6: … band 76 fences the generation") can only ever skip.
Every producer preserves its true exit via `set -o pipefail` +
`status=${PIPESTATUS[0]}` (the vitest leg's exit, not tee's).
No other wallclock file was run; no behavioral failure was retried.
The expected skip set for producer 1 is exactly the one manual test
(it.skipIf(SYNARA_T17_MANUAL_TEARDOWN !== "1")); any other skip is challenge.

6. PRODUCER RUNS (R class — EXECUTED 2026-08-28 12:23–12:54 local)
------------------------------------------------------------------
All five authorized files ran, serially, one standalone vitest process each,
from /tmp/symphony-t06/apps/server, ALFIE_REPO_DIR=/tmp/alfie-t06,
`env -u SYNARA_T17_MANUAL_TEARDOWN`, `set -o pipefail` + PIPESTATUS[0].
No other wallclock file was run; no behavioral failure was retried.

6a. piSubagentRealPiAcceptance.test.ts — Node v24.14.1 producer
  Command shape: per WP-02 §"Exact commands" (node ../../node_modules/vitest.mjs …).
  Start 12:23:39 local; end 12:31:34 local; vitest Duration 473.97s.
  PRODUCER_EXIT (PIPESTATUS[0]): 1 — LEG FAILED.
  Result line: `Tests  8 failed | 1 passed | 1 skipped (10)`.
  Skips: exactly the one MANUAL T17-AC6 destructive test
  (it.skipIf SYNARA_T17_MANUAL_TEARDOWN !== "1") — the expected skip set; no
  unexpected skip. All mandatory non-manual stages were collected and executed.
  Stage 0 (provenance + owned isolation) PASSED (1 passed).
  Failing rows (observed vs required):
    - T17-AC1 stage 1 (piSubagentRealPiAcceptance.test.ts:278): managed
      capability negotiation returned isManaged=false; required true
      ("managed_enabled" + "managed-spawn").
    - T17-AC2 stage 2 (:388 via waitFor :2145): "Timed out after 90000ms
      waiting for slow managed admission."
    - T17-AC7 stage 7 (:669, caused by :592): observed session kind
      'bridge_absent', required 'capability_mismatch'; diagnostics=[];
      modelRequests=[] — the extension-free negative control was NOT
      distinguishable from the stripped-capability control.
    - T17-AC9 stage 3 (:731): timed out waiting for managed admission.
    - T17-AC4 stage 4 (:1193, caused by :1037): timed out; modelRequests
      show hasAgentTool:false, delegated:false on every real request —
      the Agent tool was never registered in any real Pi session.
    - T17 slice 4 stage 5 (:1526, caused by :1259): timed out waiting for
      managed admission; hasAgentTool:false.
    - T17-AC6 stage 6 (:1852, caused by :1595): timed out waiting for
      managed admission; hasAgentTool:false. (This leg reaches only watchdog
      handoff bands 70–74 by design; it made no destructive claim; band 76
      was never reached and was not claimed.)
    - T17-AC8/AC9 teardown (:1877): user Pi home digest changed during the
      run: expected deecd34d35981f8932a2da3816cb59c286829a8a76ddc03b32156f1a16a955e3,
      observed 31c1b30fc01a96de975f6066bc7747aa7549fe3d01c6ac4f379b7e3d89a68b2d.
      harness.envWasRestored() was true. Post-run host inspection (12:19–12:32
      window, sessions excluded by the snapshot's own method): the only
      ~/.pi path modified in-window outside agent/sessions is
      ~/.pi/agent/fff/frecency/data.mdb (mtime 12:31:34 — the producer's own
      teardown minute). The snapshot digest walks all ~/.pi except
      agent/sessions and models-store.json, so this external-tool (Pi FFF
      frecency index) write is the observed digest delta. It was NOT caused
      by a Symphony harness write path under this record's evidence; root
      attribution beyond this observation is left to the challenge owner.
      No user live-instance content is claimed mutated by the harness.

6b. piSubagentCanonicalIdentityAcceptance.test.ts — Bun producer
  Start 12:41:12; end 12:47:15 local; vitest Duration 362.36s.
  PRODUCER_EXIT (PIPESTATUS[0]): 1 — LEG FAILED.
  Result line: `Tests  2 failed | 7 passed (9)`.
  The 7 unit-simulation tests passed; BOTH real-Pi F5 steer-race tests timed
  out at their 180s test timeouts:
    - terminal-first (:878): waiting on sdk-insertion counter after slow
      child hold — steer_subagent tool never registered (extension absent
      in the real child session).
    - enqueue-first (:881): waiting for natural live retirement/bridge
      removal after release.
  Plus 2 vitest-unhandled SocketCloseError uncaught exceptions bubbling from
  the harness dispose path (helpers :2151) after the timeouts.

6c. piSubagentLifecycleContainmentRealPiAcceptance.test.ts — Bun producer
  Start 12:47:26; end 12:51:29 local; vitest Duration 242.12s.
  PRODUCER_EXIT (PIPESTATUS[0]): 1 — LEG FAILED.
  Result line: `Tests  1 failed (1)` — test timed out at its 240s limit at
  the band-74/attemptId step (:218 region, survivors record write).
  Its emitted T03_REAL_PI_EVIDENCE shows the real composition progressed
  further here: admissions=1, delegatedModelRequests=1,
  extensionSteerEmissions=1 (one REAL steer through the extension), journal
  sequences [1,2,3,40], full band ordering 74/75/77/78 + proven-76 fence +
  stale-terminal ignored. The failure occurred after those proofs, at the
  post-terminal survivors-recording step. 1 unhandled SocketCloseError
  uncaught exception from the harness dispose path (helpers :2151).

6d. piSubagentRestartAcceptance.test.ts — Bun producer
  Start 12:53:12; end 12:53:22 local; vitest Duration 9.76s.
  PRODUCER_EXIT (PIPESTATUS[0]): 0 — LEG PASSED.
  Result: `Test Files  1 passed (1)` · `Tests  1 passed (1)` · 0 skipped.
  Executed: "T10-AC3/AC1/AC5: a real live bridge record under the same
  identity refreshes observation; the no-owner view orphans; a late fenced
  terminal is ignored and counted" (:360).

6e. piSubagentResumeAcceptance.test.ts — Bun producer
  Start 12:53:35; end 12:53:39 local; vitest Duration 3.54s.
  PRODUCER_EXIT (PIPESTATUS[0]): 0 — LEG PASSED.
  Result: `Test Files  1 passed (1)` · `Tests  1 passed (1)` · 0 skipped.
  Executed: "T14-AC1/AC4/AC6: one explicit resume creates the new child
  attempt under the same execution; no implicit path resumes" (:348).

POST-RUN GATES (all five producers complete):
  Symphony worktree: HEAD still 12fd6686edc26a3fa0382e8bdeb83a1be8045539
    (detached); tracked delta 0 lines; named-surface working diff EMPTY.
  Alfie worktree: HEAD still 3fe340b401ca86bcbe8b55abd4de107e1d93482e
    (detached); status 0 lines (clean).
  Main checkout: staged 0; status exactly the three protected WIP files +
    new WP-02 evidence artifacts; WIP aggregate diff hash re-computed
    ab8f8f54fe818819721f737aa337156ed6348c7410c55083ce3a67785bb7eaa8 — EXACT.
  Zero-delta gate (12fd6686..HEAD on the named surface): EMPTY.

7. WORKTREE DISPOSITION
-----------------------
Both worktrees remain at their exact pins with zero tracked delta after the
producers (re-verified post-run), retained for WP-03 (manual destructive run)
consumption. No `git worktree remove` executed by WP-02.

8. LEG-SET OUTCOME AND STOP
---------------------------
Verification contract requires five legs exit 0 standalone. Observed:
2/5 legs passed (restart 0, resume 0); 3/5 legs FAILED exit 1 (realpi,
canonical-identity, lifecycle-containment). Per WP-02 §Escalation ("any leg
fails" → challenge) and PLAN §11, execution stopped at this record; WP-02 was
NOT marked completed; no commit was made by the attempt-1 run; Ticket 06 did
not advance. See WP-02-nondestructive-disposition.md §4 for the
failing-row detail and the differential root-cause evidence (isolated Alfie
worktree lacks gitignored ext-local node_modules — @sinclair/typebox, croner,
nanoid, yaml — and the user checkout's built outputs, which Ticket 05's
passing legs implicitly consumed via ALFIE_REPO_DIR=/Users/anhpham99/alfie).

9. CORRECTION AMENDMENT (recorded 2026-08-28, before any rerun)
---------------------------------------------------------------
This §9 amends §6/§8 with the owner-resolved, source-grounded correction;
§1–§8 above stand unchanged as the attempt-1 record.

- Attempt-1 classification: ENVIRONMENT/RUNNER EVIDENCE, NOT a behavioral
  PASS. The three failed-leg raw logs are durably preserved byte-identical
  under new explicit names (never to be overwritten or deleted):
    WP-02-attempt-01-realpi-acceptance.log
      SHA-256 c666c1e644bcd4c2dd74cb14a4bff177ac540b22c8290af3c40da3ed492f24f2
    WP-02-attempt-01-canonical-identity-acceptance.log
      SHA-256 8543253556bf745a7fa3480582f42281e1189e4501d12e696d06b9158949876c
    WP-02-attempt-01-lifecycle-containment-realpi.log
      SHA-256 14c505321efc374a32fda4c75dbfc299c86742499c38ab764fe4be152d9a93cd
  The two passing logs keep their canonical names unchanged
  (WP-02-restart-acceptance.log bd85766c5955d429eda11d97f7b4784f04bcf93b69fc7e6b6d43096c736d935e,
  WP-02-resume-acceptance.log a115ec4dba98b4af68a5a96bdc7e5a5da1383d3357468d94ce682250e3779d29)
  and remain reusable final evidence.
- Source-grounded correction: bounded `npm ci` at
  `/tmp/alfie-t06/agent/extensions/pi-subagents` (the extension ships
  package-lock.json; entry `"pi": {"extensions": ["./src/index.ts"]}`).
  The provenance verifier (piSubagentRealPiAcceptanceHelpers.ts:561–580)
  explicitly ignores `node_modules` in `git status --porcelain`, so this is
  environment preparation, NOT an Alfie source/pin change; §1's clean-status
  and pin invariants are unaffected. Zero-tracked-delta proof required
  post-install (WP-02 §"Attempt 2" item 1).
- Runner correction: all three corrected legs run under Node v24.14.1
  (root engines node ^24.13.1; apps/server >=24.10; Node-wallclock policy),
  because attempt-1 legs 2–3 additionally failed on Bun-only Effect
  SocketCloseError schema exceptions (`code` undefined) from the harness
  dispose path (§6b/§6c; helpers :2151).
- Producer-window rule: no concurrent FFF/semantic/search or other
  agent-tool activity during the three rerun commands (bounds the §6a
  frecency-digest interference class; WP-02 §"Attempt 2" item 3).
- Scope: exactly one corrected attempt of ONLY the three failed legs, after
  the npm ci; corrected-attempt logs take the original canonical WP-02
  names; attempt-01 names above are permanent. No producer was executed as
  part of this amendment record.

10. POST-AMENDMENT GATES (at this correction commit, pre-rerun)
---------------------------------------------------------------
Main HEAD parent: 9208e17288a4dd0862c8078f4ecdd332fff02652 (WP-01 evidence
commit). Protected WIP files still modified-unstaged; aggregate diff hash
re-verified ab8f8f54fe818819721f737aa337156ed6348c7410c55083ce3a67785bb7eaa8
— EXACT. Staged before this correction commit: 0. Zero-delta gate
(12fd6686..HEAD named surface): EMPTY. Symphony worktree /tmp/symphony-t06:
12fd6686, tracked delta 0. Alfie worktree /tmp/alfie-t06: 3fe340b4, status
clean, ext-local node_modules still ABSENT (no npm ci run yet).

11. ATTEMPT-2 (CORRECTED) INTEGRATED LEG — CHALLENGE RECORD
-----------------------------------------------------------
Recorded 2026-08-28 after the exactly-one authorized corrected attempt. This
§11 amends the state after §10; §1–§10 stand unchanged. NO behavioral PASS is
claimed; the corrected integrated leg FAILED and WP-02 remains open at the
challenge gate.

11a. Environment preparation (authorized `npm ci`, exactly once) — PASS.
  `cd /tmp/alfie-t06/agent/extensions/pi-subagents && npm ci` succeeded.
  Post-install proof: `git -C /tmp/alfie-t06 status --porcelain` showed only
  gitignored `node_modules` lines; zero tracked delta; HEAD unchanged at
  3fe340b401ca86bcbe8b55abd4de107e1d93482e (detached); fixture 5/5 hash
  basis untouched (gitignored outputs only). This unblocked the pinned
  extension's runtime deps (@sinclair/typebox, croner, nanoid, yaml).

11b. Corrected integrated producer (leg 1 rerun under Node, exactly once).
  Command shape: per WP-02 §"Attempt 2" item 2 — cwd
  /tmp/symphony-t06/apps/server, ALFIE_REPO_DIR=/tmp/alfie-t06,
  `env -u SYNARA_T17_MANUAL_TEARDOWN`, `set -o pipefail` + PIPESTATUS[0],
  `node ../../node_modules/vitest/vitest.mjs run --project wallclock
  --maxWorkers=1 --no-file-parallelism
  src/provider/piSubagentRealPiAcceptance.test.ts 2>&1 | tee
  <evidence>/WP-02-realpi-acceptance.log`.
  Producer-window discipline observed: no concurrent FFF/semantic/search or
  other agent-tool activity by the executing worker between launch and exit.
  Start 13:33:00 local; vitest Duration 62.26s (tests 59.91s).
  PRODUCER_EXIT (PIPESTATUS[0]): 1 — LEG FAILED (again; different cause).
  Result line: `Tests  1 failed | 8 passed | 1 skipped (10)`.
  Skips: exactly the one MANUAL T17-AC6 destructive test — the expected skip
  set. ALL managed/non-manual behavioral stages now PASSED (8/8): stage 0
  provenance+isolation, stage 1 managed capability negotiation, stage 2
  detach+reconnect, stage 3 cancellation, slice-4 stage 4/5, stage 6
  watchdog handoff — the attempt-1 extension-load and Bun-runner failures
  are gone. The npm ci + Node correction is confirmed effective.

  Sole failing row — T17-AC8/AC9 teardown user-Pi-home digest
  (piSubagentRealPiAcceptance.test.ts:1877):
    expected 1e964fa96a4f15c10995cbdf240db5d6c5e821db4708a9b9ebfb8e33a2baebb5
    observed 472d918aaea03aff903cb5a109bb9fcd1e1af45168bc37fbd4f9d2bd53c9ebf1
  harness.envWasRestored() was true; dispose idempotent; owned temp root
  removed.

11c. Root cause (source-grounded, exact). The only host path changed during
  the producer window was ~/.pi/agent/fff/frecency/data.mdb (mtime 13:34:02
  — exactly the producer-start window; Pi FFF frecency index). Cause chain:
  the harness snapshots os.homedir()/.pi BEFORE setting only PI_CODING_AGENT_DIR
  and PI_HOME (piSubagentRealPiAcceptanceHelpers.ts:1541–1553), and the
  digest walker is os.homedir()-anchored (helpers :488–549); the outer Vitest
  process therefore still resolved user Pi settings (~/.pi/agent/settings.json
  enables `npm:@ff-labs/pi-fff`), so the Pi instance launched by the harness
  loaded the user's Pi FFF plugin, which wrote its frecency state in the user
  ~/.pi tree — outside every harness-owned directory. The digest-only failure
  is external tool state, NOT a Symphony harness write-path violation: no
  user live-instance content was mutated by the harness; envWasRestored held.

11d. Evidence preservation and classification.
  The corrected-attempt raw log is durably preserved byte-identical as
    WP-02-attempt-02-realpi-acceptance.log
    SHA-256 cf6db25f045030cb7be2949322820d283c9a32b5bf7459c44051b9ee12a9d1b0
  (canonical WP-02-realpi-acceptance.log currently holds the same bytes and
  must not be treated as a PASS record). This is environment/isolation
  evidence of a managed-stage-functional run, NOT a behavioral PASS and NOT
  a WP-02 completion.

11e. Owner-decision gate (STOP). Per WP-02 §"Attempt 2" item 5, the corrected
  attempt is spent; no further run is authorized by this record. The next
  safe correction — process-level HOME isolation for the Vitest process
  (a fresh temporary outer HOME, removing user Pi settings from discovery
  while ALFIE_REPO_DIR and all harness-owned dirs remain explicit) — requires
  a FRESH owner decision. If authorized, the owner-decided scope is one
  attempt covering the integrated leg plus the canonical-identity and
  lifecycle-containment corrected legs under Node. Canonical/lifecycle
  corrected legs were NOT run in attempt 2 and remain pending. WP-03–WP-07
  stay gated behind a five-legs-exit-0 WP-02 record.

12. AUTHORIZATION RECORD (2026-08-28, current session) — supersedes the
    §11e owner-decision gate only; no evidence in §1–§11 changes.
---------------------------------------------------------------------
The owner replied in the current session, verbatim: `Cho phép tất cả các
cổng`, authorizing the three remaining gates in the fixed conditional
sequence of PLAN §7c:
  1. WP-02 attempt 3 (exactly one, non-destructive): the three pending legs
     under Node with process-level temporary HOME isolation — fresh
     `mktemp -d` outer HOME per producer, HOME set only for that
     Node/Vitest process, EXIT-trap removal, ALFIE_REPO_DIR=/tmp/alfie-t06
     explicit, manual env unset, canonical log names,
     attempt-01/attempt-02 logs preserved byte-identical. Executable
     contract: WP-02 §"Attempt 3". No retry.
  2. WP-03 (exactly one, M): isolated manual destructive real-Pi run under
     the existing accepted recipe — CONDITIONAL on WP-02 attempt 3 PASS
     (five legs exit 0). Exact child-owner TERM→KILL scope only; no PID
     guessing, no external signalling, no retry.
  3. WP-04 (exactly one, Q): one `bun fmt` + `bun lint` + `bun typecheck`
     gate in the isolated Symphony worktree — CONDITIONAL on WP-03 PASS.
     Stop if the formatter touches out-of-scope paths.
Attempt-3 stop gates (any one stops immediately, no retry): outer-HOME
cleanup failure; pin drift (12fd6686 / 3fe340b4); protected WIP hash drift
from ab8f8f54fe818819721f737aa337156ed6348c7410c55083ce3a67785bb7eaa8;
non-empty Pi acceptance-surface zero-delta gate; unexpected skip in any
leg; any nonzero producer exit.
Interpretation: §10 attempt-01 canonical/lifecycle logs remain historical
environment/runner evidence and are NOT current; the §11 attempt-02 log
remains a non-PASS environment record. This record authorizes execution
only and contains no behavioral result.

13. ATTEMPT-3 DATED ERRATUM — 2026-08-28
------------------------------------------------------------------------
This section supersedes the earlier attempt-3 authorization record; its
authorization was spent by the historical exit-1 run and is non-transferable.

------------------------------------------------------------------------
Decision 0007 is binding and aspect-scoped Authoritative for the fixture
correction, candidate rebaseline, attempt-3 erratum, and downstream gate state.

Attempt 3 failed at piSubagentRealPiAcceptance.test.ts:1125, where the test
required the two acknowledged outbox rows to share one durable dispatchBatchId.
The rows instead had two distinct batch IDs. The test stopped before the later
public follow-up replay/count assertion, so attempt 3 did not observe or
establish that two public follow-up message events were emitted. No duplicate
follow-up count was observed.

The line-1125 assertion compared Set(dispatchBatchIds).size. The observed
values were two distinct durable batch IDs among acknowledged outboxes. Preserve
attempt-3 exit 1, stopped status, and raw-log SHA-256
798148d1944242b68014e753fe05a15aec947cf22376f6c7ec6248887cbd0f99. Do not
claim two public follow-ups or a production duplicate-follow-up defect.

14. CURRENT REBASELINE AND ROUTE
------------------------------------------------------------------------
Historical Symphony base: 12fd6686edc26a3fa0382e8bdeb83a1be8045539
Historical WP-01: 9208e1728 (supporting only)
Future candidate: exact two-file child, changing only:
  apps/server/src/provider/piSubagentRealPiAcceptanceHelpers.ts
  apps/server/src/provider/piSubagentRealPiAcceptance.test.ts
The candidate base→child delta is exactly those two files; a third file,
production/configuration/manifest/lockfile change, or Alfie change is forbidden.
After freeze, renewed evidence has zero source delta.

Route: exact two-file candidate → freeze → renewed WP-01 same 19 files 296/296
→ one renewed full five-file WP-02 → fresh owner auth WP-03 → fresh owner auth
WP-04 conditional on new WP-03 PASS → WP-05 one review → WP-06 Decision 0009
→ WP-07. Old WP-03/WP-04 authorization is unspent, non-transferable, and not
executable. WP-05/WP-06 reservations are unused. No current PASS is claimed.

15. PROTECTED WIP
------------------------------------------------------------------------
Required aggregate diff hash:
ab8f8f54fe818819721f737aa337156ed6348c7410c55083ce3a67785bb7eaa8
Protected owner WIP remains unstaged and untouched.

16. RENEWED WP-02 ATTEMPT — CANONICAL-IDENTITY CHALLENGE
----------------------------------------------------------
Recorded: 2026-08-28 (local, UTC+7), after the renewed WP-02 attempt. This
section records the renewed result; historical evidence above remains
unchanged and immutable.

Frozen candidate / producer identity:
  Symphony worktree: /private/tmp/symphony-t06
  candidate HEAD: ffd45bd867e94c9003415f5f2e937cc9c616e399 (exact)
  Alfie worktree: /tmp/alfie-t06
  Alfie HEAD: 3fe340b401ca86bcbe8b55abd4de107e1d93482e (exact)
  Alfie status: clean / zero tracked delta (post-attempt re-verified)
  producer runner: Node v24.14.1 for both renewed legs
  manual env: SYNARA_T17_MANUAL_TEARDOWN unset for both legs
  producer window: no concurrent FFF/semantic/search or other tool activity
  was permitted while either producer was running; the worker only waited
  for the process to exit.

HOME isolation and cleanup:
  integrated leg: fresh outer HOME tmp.gLZPdTzMaC; EXIT cleanup completed;
  post-run absence verified.
  canonical leg: fresh outer HOME tmp.wX79oSZqVb; EXIT cleanup completed;
  post-run absence verified.
  HOME was scoped to each Node/Vitest process only. No shared HOME was used.

Renewed leg results (exactly once, serially):
  1. integrated — PASS, exit 0. One file passed; 10 tests passed; one
     expected manual skip. The exact-two T17-AC4 fixture barrier accepted
     accepted=2 and pending=2. Log SHA-256:
     5bdb1a515b715fa62b63602f83aa1762b9b61812f194528c2322b71dbc5bb78b
  2. canonical-identity — FAILED, exit 1. One file failed; 2 tests failed
     and 7 passed. The terminal-first assertion at test.ts:913 expected
     diagnostic pi_subagent_read_live_record_unavailable but observed
     pi_subagent_live_lifecycle_stale_ignored. The enqueue-first assertion at
     test.ts:924 expected text `Steer state: applied` but observed
     `Managed live lifecycle stale response ignored`.
     Log SHA-256:
     28ded0c0221643cff775b467c9f2df15cf83627016a58e9184c59c46e170c08c

Atomic stop and gate state:
  The renewed attempt stopped immediately after the canonical-identity exit 1.
  Lifecycle-containment, restart, and resume were not run in this renewed
  attempt. No current WP-02 PASS exists; no retry is authorized. Source
  diagnosis is pending a fresh owner decision. WP-03 remains blocked pending a
  renewed five-leg WP-02 PASS and fresh authorization; WP-04 remains blocked
  pending a newly authorized WP-03 PASS. No destructive, quality, review, or
  Supervisor producer was run. These renewed logs are preserved byte-for-byte
  and must not be rewritten or interpreted as a complete WP-02 PASS.
