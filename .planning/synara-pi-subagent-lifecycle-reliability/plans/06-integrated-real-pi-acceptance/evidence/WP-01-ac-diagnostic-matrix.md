# WP-01 — AC / diagnostic matrix (19-file deterministic integrated evidence)

**Candidate SHA:** `12fd6686edc26a3fa0382e8bdeb83a1be8045539` (detached worktree `/tmp/symphony-t06`).
**Producers:** (a) one serialized Vitest invocation `bun run ../../node_modules/vitest/vitest.mjs run --project unit --maxWorkers=1 --no-file-parallelism` over 18 files from `/tmp/symphony-t06/apps/server`; (b) `bun run test src/piSubagents.test.ts` from `/tmp/symphony-t06/packages/contracts`. Exact commands in `WP-01-worktree-provenance.txt`.
**Producer exits:** `0` and `0` — `Test Files 18 passed (18)` / `Tests 256 passed (256)`; `Test Files 1 passed (1)` / `Tests 40 passed (40)`. Vitest `v4.1.10`. Full logs: `WP-01-deterministic.log`, `WP-01-contracts.log`.
**Evidence class of every row below:** **D** (deterministic, executed in this record's two invocations). **R legs are explicitly pending (WP-02)** — noted per criterion.

Every named case quoted below exists verbatim in the frozen candidate's test files (all file:line locators verified against `/tmp/symphony-t06`) and was executed green in the producer run (256+40 passed, zero failed, zero skipped). Every positive row is paired with its material failure/diagnostic row(s). Line numbers marked `C:` refer to `packages/contracts/src/piSubagents.test.ts`.

---

## T06-AC2 — Public executionId usable through detached output, durable read, terminal, reconnect, control (D; R pending WP-02)

| Aspect | Positive evidence (D) | Material failure / diagnostic evidence (D) |
|---|---|---|
| Control (cancel/steer) via public identity | `"T06-AC2: targets every managed child declaring the parent-turn scope, both transport modes"` — `piSubagentCancellationCoordinator.test.ts:202`; `"applies a valid bounded controlled steer success"` — `piSubagentCanonicalRouting.test.ts:564`; exact-tuple capture/apply of one live observation — `piSubagentLiveLifecycleContainment.test.ts:25` | `"keeps steer exact-live-only and never invokes provider for stale durable tuples"` — `piSubagentCanonicalRouting.test.ts:475`; `"returns unavailable and never dispatches for missing, mismatched, inactive, or disposed routes"` — `piSubagentLiveLifecycleContainment.test.ts:92`; foreign-attempt ack is not termination evidence — `piSubagentCancellationCoordinator.test.ts:300` |
| Durable bounded read via public identity | `"T12-AC1: an authorized read returns the bounded result with project/thread authority verified"` — `piSubagentExecutionReadBoundary.test.ts:140`; `"T12-AC3: transcript reads page by cursor through the authorized boundary"` — `:260` | `"T12-AC2: an unknown executionId denies without leaking metadata, ref, or content"` — `:170`; `"a thread/project mismatch denies exactly like an unknown id"` — `:196`; `"a snapshot failure fails closed with the stable read-denied diagnostic"` (`pi_subagent_read_denied`) — `:238` |
| Terminal via public identity | `"T07-AC1: first terminal is journaled and applied before notification"` — `piSubagentTerminalLifecycle.test.ts:105`; `"T08-AC1: terminal persistence and outbox creation are atomic before notification"` — `piSubagentCompletionOutbox.test.ts:156` | `"T08-AC1 (failure direction): outbox write failure rolls back the whole terminal transaction — never terminal without outbox"` — `piSubagentCompletionOutbox.test.ts:193`; `"T07-AC1 (failure surface): terminal persistence failure never notifies and degrades control health"` — `piSubagentTerminalLifecycle.test.ts:489` |
| Reconnect via public identity | `"resumes from a cursor by replaying exactly the gap without a snapshot"` — `wsSnapshotLiveStream.test.ts:110`; `"emits the snapshot first, the fenced replay next, and newer live events last"` — `:45`; bounded card aggregate on thread-detail snapshot — `piSubagentExecutionCardSurface.test.ts:864`; card events ride the thread-detail stream — `:1428` | Resnapshot fallbacks: cursor gap over replay limit `:186`, cursor ahead of durable head `:212`, missing resume subject `:238`, unbounded attach gap rejects with `ORCHESTRATION_RESNAPSHOT_REQUIRED` `retryable:true` `:288` (`wsSnapshotLiveStream.test.ts`); durable-first read when live route absent — `"returns durable terminal evidence without calling the provider"` `piSubagentCanonicalRouting.test.ts:357`, `"preserves the durable result when the exact live observation route is absent"` `:161` |
| Public-surface hygiene | diagnostic whitelist (`pi_subagent_result_truncated`), arbitrary provider strings suppressed — `piSubagentCanonicalRouting.test.ts:429`; `"never exposes internal reasons or provider text on the public result surface"` — `piSubagentLiveLifecycleContainment.test.ts:617`; bounded card without prompt/raw progressJson — `piSubagentExecutionCardSurface.test.ts:864` | `"T12-AC5: full result/transcript content never enters lifecycle events, snapshots, or the WS push payload"` — `piSubagentExecutionCardSurface.test.ts:1447`; contract-level exclusion row `C:863` (`keeps full result/transcript content out of lifecycle events, execution cards, and outbox entries`) |
| Fixed codes/messages | `pi_subagent_read_denied` (`piSubagentExecutionReadBoundary.test.ts:726`); `ORCHESTRATION_RESNAPSHOT_REQUIRED` (`wsSnapshotLiveStream.test.ts:288`); contracts diagnostic-code literal families `C:702,715,731,745,846` | Denial mapping stable: `"T12-AC1/AC2: WS denial mapping is stable for unknown and unauthorized reads"` — `piSubagentExecutionReadBoundary.test.ts:716` |

**R legs remaining for T06-AC2 (WP-02):** real-Pi control (cancel/steer) through the pinned controlled Alfie extension, real detached-output/progress observation, and real reconnect over WS — pending, non-destructive class.

## T06-AC3 — Real lifecycle: progress, terminal-before-cleanup, cancellation, watchdog handoff, truthful diagnostics (D; R pending WP-02)

| Aspect | Positive evidence (D) | Material failure / diagnostic evidence (D) |
|---|---|---|
| Progress | `"reportObservation accepts progress and heartbeat observation inputs"` — `piSubagentBridge.test.ts:563`; `"accepts a valid progress + heartbeat policy"` — `:446`; safe finite turn-counter projection — `piSubagentExecutionCardSurface.test.ts:937` | Policy sanitization failure direction: `"strips invalid rateHz but keeps a valid heartbeat policy"` `:511`, `"strips fractional or out-of-range heartbeat interval/lease"` `:530` (`piSubagentBridge.test.ts`); `"T07-AC6: terminal persists while the observation sink is degraded (progress throws)"` — `piSubagentTerminalLifecycle.test.ts:370` |
| Terminal-before-cleanup | `"records no-owner teardown evidence before the Ticket-10 orphan fence"` — `piSubagentStartupRecoveryOrder.test.ts:116` (order proven: outbox recovery → band-78 `owner_unproven` at generation 1 → orphan fence with exactly one generation advance); `"T16-AC6 (boundary): a same-generation terminal arriving between handoff and teardown settles as ordinary lifecycle terminal"` — `piSubagentProcessTeardown.test.ts:662` | `"T16-AC3: only a liveness-verified dispatch settles — a kill API return without proof (survivors) never settles"` — `piSubagentProcessTeardown.test.ts:270`; `"T15-AC3/AC4 (F1): terminal evidence arriving during the stage-2 evidence window stops escalation before any session stop"` — `piSubagentWatchdogEscalation.test.ts:702` |
| Cancellation | `"T06-AC1: records durable cancelling intent BEFORE dispatch and settles cancelled only from a same-identity ack"` — `piSubagentCancellationCoordinator.test.ts:100`; idempotent replay `:156`; sibling isolation `"T11-AC6: cancels exactly the requested execution, leaves siblings untouched"` `:616` | `"T06-AC6: dispatch failure preserves cancelling, stable diagnostic, bounded retry, escalation without success claim"` — `:426`; `"T06-AC6: absent bridge (mixed-version extension) is a stable dispatch failure, never a silent skip"` — `:474`; `"T06-AC3: extension reporting stale maps to the stale_generation outcome without settling"` — `:535` |
| Watchdog handoff | `"T15-AC6: session-stop timeout produces a stable diagnostic and hands the owned execution to the process-teardown stage"` — `piSubagentWatchdogEscalation.test.ts:549`; band-60 trigger `"T15-AC1: wall-time expiry consumes the band-60 trigger…"` `:187`; sweep chain `"escalates wall-time-expired execution through stage controls"` — `piSubagentWatchdogSweep.test.ts:79`; adapter wiring `"T16-AC1/AC2: the adapter-mounted sweep dispatches the owned teardown once for a handed-off execution and settles proven with the fence"` — `piSubagentTeardownWiring.test.ts:203`; owner-endpoint retention `"routes exact execution identity through the retained owner endpoint and settles proven"` — `piSubagentChildOwnerTeardownWiring.test.ts:450` | `"T16-AC4 / Decision 0029: production AggregateError survivor evidence reaches band 77 and the operator diagnostic"` — `piSubagentProcessTeardown.test.ts:347`; `"missing PID evidence is reported as unavailable, never as zero survivors"` `:405`; `"Decision 0033 §6: a thrown owned-teardown dispatch records non-terminal owner_unproven band 78, never synthetic survivors"` `:837`; owner-timeout band 78 — `piSubagentChildOwnerTeardownWiring.test.ts:797`, invalid owner replies → band 78 `:616`; proven-outcome write failure retains owner for retry — `:942` |
| Truthful diagnostics | `"stage records carry per-stage diagnostic codes (trigger entry, timeout, stopped, cleanup-uncertain)"` — `piSubagentWatchdogEscalation.test.ts:1070`; operator observers receive safe metadata — `piSubagentWatchdogSweep.test.ts:178`; `"settles a proven owned teardown through the driver"` — `piSubagentProcessTeardownSweep.test.ts:51` | `"T15-AC5: timer-only progression never claims stopped or cancelled; projection stays cancelling through every stage"` — `piSubagentWatchdogEscalation.test.ts:472` (+ projection row `piSubagentExecutionCardSurface.test.ts:1034`); `"journal stage-record persistence failures surface a stable diagnostic and never wedge the escalation chain"` `:1002`; `"T07-AC3: sequence gap emits a stable diagnostic without deleting/delaying the terminal"` — `piSubagentTerminalLifecycle.test.ts:192`; `"Decision 0001 failure coverage: a repository outage on the request write reports the stable persistence-failed diagnostic"` — `piSubagentProcessTeardown.test.ts:808` |

**R legs remaining for T06-AC3 (WP-02):** real-Pi progress/heartbeat flow, real cancellation ack through the controlled extension, real watchdog/teardown handoff observation — pending.

## T06-AC4 — Restart/reconnect restores terminal/live-owner/orphan truth; no auto replay; explicit Resume proven or truthfully denied (D; R pending WP-02)

| Aspect | Positive evidence (D) | Material failure / diagnostic evidence (D) |
|---|---|---|
| Restart restores terminal truth | `"T10-AC2: a transcript terminal marker with matching identity and generation restores the terminal outcome instead of orphaning"` — `piSubagentRestartReconciliation.test.ts:352` | Identity/generation guard rejects mismatched markers (same test); `"F2: recovery excludes inapplicable stale terminals (no transiently-pending stale entries)"` — `:691`; `"terminal aggregates are never orphaned (owner loss cannot reverse terminal truth)"` — `:620` |
| Restart restores live-owner truth | `"T10-AC3: a live-owner listActive match under the same execution, attempt, and generation refreshes observation without a new attempt"` — `piSubagentRestartReconciliation.test.ts:233` | Absent live route: `"returns unavailable…"` classification family — `piSubagentLiveLifecycleContainment.test.ts:92`; late-response/retirement races cannot fabricate observations `:300,576` |
| Orphan truth | Non-terminal orphan with stable owner-loss diagnostic `pi_subagent_owner_loss_orphaned` — `"T10-AC1: no live-owner or terminal evidence produces non-terminal orphaned…"` — `piSubagentRestartReconciliation.test.ts:132`; exactly-one generation advance asserted in `piSubagentStartupRecoveryOrder.test.ts:116`; configurable threshold `:595` | `"T10-AC6: the orphan diagnostic explains partial side effects and recommends inspection"` — `:203`; `"T10-AC7: lease expiry without renewed live-owner evidence enters the same orphan path"` — `:508`; dead-owner-but-active child is NOT proof — `piSubagentCancellationCoordinator.test.ts:570` |
| No automatic replay | `"T10-AC4: startup reconciliation performs no spawn, resume, or other side-effecting delegation replay"` — `piSubagentRestartReconciliation.test.ts:310`; `"T14-AC3: reconciliation NEVER resumes…"` — `piSubagentResumeCoordinator.test.ts:651`; public command-surface audit — `:716` | `"T16-AC7: restart with no live owned supervisor dispatches nothing, kills nothing, and records the bounded owner_unproven"` — `piSubagentProcessTeardown.test.ts:703`; adapter-boundary variant `piSubagentTeardownWiring.test.ts:260`; restart with empty registry maps missing/disposed owners to undefined — `piSubagentChildOwnerTeardownWiring.test.ts:1145` |
| Explicit Resume proven | `"T14-AC1: resume keeps the executionId, mints ONE new attemptId, advances generation, and records the new generation BEFORE the child starts"` — `piSubagentResumeCoordinator.test.ts:202`; idempotent duplicate `:299`; settlement state machine `:751`; card shows new attempt with prior evidence `:509` | Truthful denials: non-orphaned `invalid_state` `:558`, unknown `not_found` `:606`, unmanaged session `:538`, thread-authorization/subject-authority/quota gate denials `:405/:437/:457`, child-start failure leaves honest durable queued attempt with `child_start_failed` `:624`; stale-generation fencing `piSubagentExecutionReadBoundary.test.ts:535,602,607` |
| Bridge/capability truthfulness at restart | `"probe is idempotent and produces no side effects on repeated calls"` — `piSubagentBridge.test.ts:145`; canonical binding acceptance `:211` | `bridge_absent` `:68`, `bridge_error` `:79`, `bridge_malformed_response` `:90`, `capability_mismatch` on missing capabilities `:102` and omitted-in-success `:123`, offered-vs-supported context `:49`, older extension → mismatch + unmanaged fallback `:173` (`piSubagentBridge.test.ts`) |

**R legs remaining for T06-AC4 (WP-02):** real-Pi restart/reconnect against the controlled worktree (terminal restore, live-owner refresh, orphan, explicit Resume) — pending.

## T06-AC5 — Stale attempt/generation and duplicate delivery/control fenced and bounded (D; R pending WP-02)

| Aspect | Positive evidence (D) | Material failure / diagnostic evidence (D) |
|---|---|---|
| Stale attempt/generation terminal | Stale rows are journaled history-only with aggregates untouched: `"T07-AC4: stale attempt/generation terminal ignored, counted, truth preserved"` — `piSubagentTerminalLifecycle.test.ts:266`; `"T10-AC5: late events from stale attempts or generations after reconciliation are ignored and counted"` — `piSubagentRestartReconciliation.test.ts:437` | `"recordTeardownOutcome on a stale generation journals history only and never fences the newer attempt"` — `piSubagentProcessTeardown.test.ts:953`; `"recordTeardownOutcome(proven) on an already-terminal aggregate journals history only"` `:991`; `"T14-AC2: late events, terminals, cancels, and completions from the superseded attempt are ignored (generation fence) and counted"` — `piSubagentResumeCoordinator.test.ts:323`; `"T06-AC3: stale late settlement cannot affect a newer attempt (journaled history only)"` — `piSubagentCancellationCoordinator.test.ts:272` |
| Duplicate delivery fenced | `"T08-AC3: replayed terminal and replayed outbox processing create no duplicate entry or effect"` — `piSubagentCompletionOutbox.test.ts:327`; dedupe-identity bounded retry `:480` | Retry-budget exhaustion stops auto-recovery — `:536`; `"T08-AC2 (supersede guard): an acknowledged entry cannot be superseded"` — `:657`; `"T08-AC6: a completion superseded by a newer generation produces no delivery effect; evidence stays readable"` — `:571` |
| Duplicate control fenced | `"T11-AC6: idempotent re-cancel replays already_applied intent and never re-dispatches; unknown execution reports not_found without state writes"` — `piSubagentCancellationCoordinator.test.ts:690`; `"T15-AC6 (F3): a same-generation terminal arriving after the teardown handoff still settles through the normal lifecycle (no premature fence)"` — `piSubagentWatchdogEscalation.test.ts:906` | `"T06-AC4/AC5: an ack carrying a different attempt/generation is not termination evidence — state stays cancelling"` — `piSubagentCancellationCoordinator.test.ts:300`; `"T06-AC3: extension reporting stale maps to stale_generation without settling"` — `:535`; watchdog idempotency `"re-running the watchdog after an uncertain escalation does not duplicate stage records"` — `piSubagentWatchdogEscalation.test.ts:1125`; `"T16-AC2 (truthful replay): a proven replay onto an already-fenced aggregate reports the existing fence without claiming…"` — `piSubagentProcessTeardown.test.ts:567` |
| Live-route stale fencing | `"retires only by exact registration identity: a stale equal tuple cannot retire its replacement"` — `piSubagentLiveLifecycleContainment.test.ts:576`; collision-free tuple keys `:240`; sibling/equal-tuple isolation `:53` | `"does not expose a value that resolves after an explicit timeout marker"` — `:206`; `"revalidates after await and permanently ignores a retired late response"` — `:300`; `"keeps observation free of provider_acceptance and outcome_unknown for every failure shape"` — `:501` |
| Contract-level fencing | Canonical identity + equal-only alias, bounded — `C:49,66` (contracts suite); `"T02: canonical identity, equal-only alias, and stale tuple fencing are durable-first"` — `piSubagentExecutionReadBoundary.test.ts:535` | `"rejects cancel commands with malformed identity or non-positive generation"` — `C:1031`; `"rejects provider-shaped diagnostic additions"` — `C:43`; identity-mismatched owner response → `owner_unproven`, never proven — `piSubagentBridge.test.ts:947` |

**R legs remaining for T06-AC5 (WP-02):** real-Pi stale-generation and duplicate-delivery/control fencing under the pinned composition — pending.

## T06-AC7 — Every stage failure reports stage and stable diagnostic; mock-only success impossible (D; R pending WP-02)

| Aspect | Positive evidence (D) | Material failure / diagnostic evidence (D) |
|---|---|---|
| Stage-attributed stable diagnostics | `"stage records carry per-stage diagnostic codes (trigger entry, timeout, stopped, cleanup-uncertain)"` — `piSubagentWatchdogEscalation.test.ts:1070`; bridge stage codes: `bridge_absent` `piSubagentBridge.test.ts:68`, `bridge_error` `:79`, `bridge_malformed_response` `:90`, `capability_mismatch` `:102,:123`; read-stage codes: `pi_subagent_read_denied` `piSubagentExecutionReadBoundary.test.ts:726,:254`, `pi_subagent_result_truncated` `:330`, `pi_subagent_transcript_missing` `:363,:467`, `pi_subagent_transcript_corrupt` `:400` | `"T12-AC7: a missing artifact reports the stable missing diagnostic without changing the outcome"` — `piSubagentExecutionReadBoundary.test.ts:340`; `"corrupt artifact lines degrade the read with a stable diagnostic"` — `:373`; `"T08-AC2 (failed_retryable): delivery failure stays retryable and never mutates terminal truth"` — `piSubagentCompletionOutbox.test.ts:281`; `pi_subagent_resume_persistence_failed` / `child_start_failed` denial stages — `piSubagentResumeCoordinator.test.ts:624` (+ AC4 row) |
| No diagnostic invention / provider-text leakage | Contracts fix the diagnostic vocabulary as literal families: Ticket 08 `C:702`, Ticket 15 `C:715`, Ticket 16 `C:731`, Ticket 13 `C:745`, Ticket 12 `C:846`; `"accepts the four fixed live lifecycle/late-terminal diagnostic codes"` — `C:32` | `"rejects provider-shaped diagnostic additions"` — `C:43`; `"whitelists provider diagnostic codes and suppresses arbitrary provider strings"` — `piSubagentCanonicalRouting.test.ts:429`; `"never exposes internal reasons or provider text on the public result surface"` — `piSubagentLiveLifecycleContainment.test.ts:617`; conservative outcome-unknown classification for malformed/thrown steer — `piSubagentCanonicalRouting.test.ts:578,588,602` |
| No synthetic success from uncertainty | Watchdog timer-only progression never claims stopped/cancelled — `piSubagentWatchdogEscalation.test.ts:472` (+ projection `piSubagentExecutionCardSurface.test.ts:1034`); `owner_unproven`/`cleanup_uncertain`/`survivors` stay non-terminal across `piSubagentProcessTeardown.test.ts:270,347,405,837`, `piSubagentBridge.test.ts:811,826,842,934,960`, `piSubagentChildOwnerTeardownWiring.test.ts:616,797` | Cancellation without proof never settles — `piSubagentCancellationCoordinator.test.ts:389,426,474,570`; `"T08-AC4: crash between terminal persistence and delivery leaves the execution terminal and the outbox recoverably pending"` — `piSubagentCompletionOutbox.test.ts:390` (evidence gap reported, never invented); journal-first recovery `:427` |
| Mock-only success impossible (structural, this ticket) | The D class by construction cannot mint a real-Pi claim: no wallclock/real-Pi file is in this 19-file set; `piSubagentRealPiAcceptance.test.ts` and the acceptance producers are wallclock-class only (PLAN §7: standalone `node` runs, controlled Alfie worktree, `node:sqlite` on Node ≥24.10 — Bun 1.3.12 cannot load it). Ticket 06 R evidence exists only via WP-02's five standalone real-Pi legs; M evidence only via WP-03. This WP records **no** real-Pi claim. | The provenance gates that make a fake real-Pi pass detectable are themselves executed: Alfie worktree pinned + clean + 5/5 fixture-hash matches + remote-URL match (`WP-01-worktree-provenance.txt` §2), and the candidate's zero source delta from `12fd6686` on the whole acceptance surface. |

**R legs remaining for T06-AC7 (WP-02):** the five standalone real-Pi wallclock legs carry the R-class stage-failure diagnostics and the mock-only-impossibility proof under real Pi.

---

## Per-file executed-case inventory (D, all green)

| File (relative to `/tmp/symphony-t06/apps/server` unless noted) | Cases | Named anchors |
|---|---|---|
| `src/provider/piSubagentCancellationCoordinator.test.ts` | 14 | :100, :156, :202, :272, :300, :340, :389, :426, :474, :506, :535, :570, :616, :690 |
| `src/provider/piSubagentWatchdogEscalation.test.ts` | 16 | :187, :253, :311, :370, :404, :472, :549, :619, :671, :702, :816, :906, :976, :1002, :1070, :1125 |
| `src/provider/piSubagentProcessTeardown.test.ts` | 19 | :198, :233, :270, :317, :347, :405, :427, :501, :567, :601, :662, :703, :758, :808, :837, :915, :953, :991, :1026 |
| `src/provider/piSubagentWatchdogSweep.test.ts` | 4 (Effect) | :79 stage-control escalation, :131 fresh heartbeat untouched, :178 observer-safe metadata, :246 timer schedules/reschedules |
| `src/provider/piSubagentProcessTeardownSweep.test.ts` | 2 | :51 proven owned teardown via driver (Effect), :144 periodic timer stops cleanly |
| `src/provider/piSubagentBridge.test.ts` | 54 | :32–:102 handshake/diagnostic family, :123–:204 capability gating, :211–:358 binding validation, :367–:563 binding/policy/observation, :615–:645 optional child-bash ownership, :659–:987 owner-teardown result validation incl. :811, :826, :842, :881, :911, :934, :947, :960 |
| `src/provider/piSubagentLiveLifecycleContainment.test.ts` | 17 | :25, :53, :92, :135, :206, :240, :267, :300, :333, :355, :387, :419, :455, :501, :542, :576, :617 |
| `src/provider/piSubagentChildOwnerTeardownWiring.test.ts` | 8 | :450, :542, :616, :797, :942, :1066, :1145, :1185 |
| `src/provider/piSubagentTeardownWiring.test.ts` | 2 | :203, :260 |
| `src/provider/piSubagentCanonicalRouting.test.ts` | 23 | :52, :101, :129, :161, :198, :204, :236, :262, :304, :329, :357, :388, :429, :475, :546, :564, :578, :588, :602, :614, :629, :648, :665 |
| `src/orchestration/Layers/piSubagentExecutionCardSurface.test.ts` | 18 | :196, :240, :297, :407, :486, :589, :652, :742, :864, :937, :1034, :1117, :1170, :1262, :1309, :1367, :1428, :1447 |
| `src/provider/piSubagentRestartReconciliation.test.ts` | 12 | :132, :203, :233, :310, :352, :437, :508, :595, :620, :649, :691, :719 |
| `src/provider/piSubagentStartupRecoveryOrder.test.ts` | 1 | :116 |
| `src/provider/piSubagentResumeCoordinator.test.ts` | 14 | :202, :299, :323, :405, :437, :457, :509, :538, :558, :606, :624, :651, :716, :751 |
| `src/provider/piSubagentTerminalLifecycle.test.ts` | 13 | :105, :143, :192, :233, :266, :327, :370, :410, :455, :489, :514, :553, :578 |
| `src/provider/piSubagentCompletionOutbox.test.ts` | 11 | :156, :193, :230, :281, :327, :390, :427, :480, :536, :571, :657 |
| `src/wsSnapshotLiveStream.test.ts` | 14 | :14, :45, :76, :110, :150, :186, :212, :238, :265, :288, :325, :355, :390, :418 |
| `src/provider/piSubagentExecutionReadBoundary.test.ts` | 14 | :140, :170, :196, :238, :260, :307, :340, :373, :405, :483, :535, :607, :630, :716 |
| `packages/contracts/src/piSubagents.test.ts` (from `/tmp/symphony-t06/packages/contracts`) | 40 | :32, :43, :49, :66, :74, :93, :107, :124, :142, :162, :181, :232, :306, :315, :326, :339, :352, :406, :439, :487, :558, :586, :607, :620, :636, :664, :702, :715, :731, :745, :769, :783, :803, :825, :846, :863, :875, :910, :965, :1031 |
| **Total** | **256 + 40 = 296** | all executed, all passed |

## AC-leg status summary

| Criterion | D (WP-01, this record) | R (WP-02) | M |
|---|---|---|---|
| T06-AC2 | **PASS** (executed) | pending | n/a |
| T06-AC3 | **PASS** (executed) | pending | n/a |
| T06-AC4 | **PASS** (executed) | pending | n/a |
| T06-AC5 | **PASS** (executed) | pending | n/a |
| T06-AC7 | **PASS** (executed) | pending | n/a |
| T06-AC6 | D-leg supported inside AC3/AC5 rows (teardown/watchdog files) | pending | pending (WP-03, exactly-one) |

No criterion above is claimed complete: R legs are pending in WP-02, and T06-AC6 additionally requires the WP-03 operator run. This matrix cites only cases executed by the two recorded producer invocations at the frozen candidate; no source, test, configuration, or Alfie file was modified by WP-01.
