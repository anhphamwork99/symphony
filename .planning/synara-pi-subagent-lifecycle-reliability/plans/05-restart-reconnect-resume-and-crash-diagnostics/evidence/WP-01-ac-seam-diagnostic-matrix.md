# WP-01 — AC / seam / diagnostic matrix (deterministic evidence)

**Candidate SHA:** `7521b92c7cb8a614346f994e963aa379175f540b`
**Producer:** one serialized Vitest invocation — `bun run ../../node_modules/vitest/vitest.mjs run --project unit --maxWorkers=1 --no-file-parallelism` over the nine listed files, run from `apps/server` in `/Users/anhpham99/symphony` (see `WP-01-workspace-state.txt` for the isolation note).
**Producer exit:** `0` — `Test Files 9 passed (9)`, `Tests 118 passed (118)`, Vitest `v4.1.10`, duration 13.90s. Full log: `WP-01-focused-deterministic.log` (footer records candidate SHA and `PRODUCER_EXIT:0`).

Rows below cite actual named test cases (quoted verbatim from the candidate test files) and the symbols they assert. Dynamic rows were executed in the single producer invocation. Structural-only assertions, notably the ProviderService inactive-runtime pre-adapter gate, are explicitly labeled and rest on executable source order plus caller/import reachability rather than an unexecuted command or a comment.

---

## T05-AC1 — Reconnect hydrates bounded durable execution truth; no new attempts or dispatch

| Aspect | Evidence |
|---|---|
| Source seam | `makeCursorSafeSnapshotLiveStream` (`apps/server/src/wsSnapshotLiveStream.ts`); `ProjectionSnapshotQuery` card reads via `system.repository.getExecutionCard` / `readThreadEvents` (`apps/server/src/orchestration/Layers/piSubagentExecutionCardSurface.test.ts` fixture seam `:182-196`); execution-card read shape in `apps/server/src/persistence/Services/PiSubagentExecutionRepository.ts` (read set) |
| Normal behavior | `"resumes from a cursor by replaying exactly the gap without a snapshot"` — `snapshotLoaded` stays `false`, `replayRange === [3, 5]`, cursor gap `[30, 40]` with existing subject replays exactly `[30, 40]` (`wsSnapshotLiveStream.test.ts:110-149, 265-287`). `"emits the snapshot first, the fenced replay next, and newer live events last"` orders snapshot → replay → live with nothing dropped (`:45-75`). |
| Material failure / diagnostic | `"falls back to the snapshot when the cursor gap exceeds the replay limit"` (only the snapshot-fence replay ran; `replayRanges === [[highWater, highWater]]`); `"falls back to the snapshot when the cursor is ahead of the durable head"` (untrusted cursor never replayed); `"falls back to the snapshot when the resume subject no longer exists"`; `"requires a fresh snapshot instead of replaying an unbounded attach gap"` → rejects with `{ code: "ORCHESTRATION_RESNAPSHOT_REQUIRED", retryable: true }`, `replayStarted === false` (`wsSnapshotLiveStream.test.ts:186-324`). |
| Fixed code/message | `ORCHESTRATION_RESNAPSHOT_REQUIRED` with bounded report `{ snapshotSequence, highWaterSequence, replayCount, replayLimit }`. |
| Durable state effect | None: the stream layer reads durable state only; the failure fallbacks re-snapshot through the existing explicit recovery path. |
| Attempt/generation effect | **Zero.** No test in the nine files observes an attempt or generation mutation from any snapshot/cursor/hydration path; the resume coordinator is the only attempt-minting seam (see AC4) and no test drives it from a reconnect path. |
| Dispatch/launch count | **0 launches, 0 admissions, 0 provider dispatches** from snapshot/cursor paths: replay fixtures emit journal `event(sequence)` objects only; no `launchChildAttempt`, no `recordAdmission`, no adapter call appears in any snapshot/cursor test (structural: `makeCursorSafeSnapshotLiveStream` input surface is `subscribeLive/snapshot/replay` streams only — `wsSnapshotLiveStream.test.ts:14-18`). |
| Bounds/redaction | Snapshot cards bounded and redacted — see bounds row below (`PI_SUBAGENT_EXECUTION_CARD_MAX_PER_THREAD`, progress/diagnostic 512-char caps, prompt/`progressJson` exclusion asserted by serialized NOT-contains). |
| Test result | All listed cases pass. Producer exit 0. |

Work-creation counters (required matrix): across every reconnect/cursor/snapshot case the execution-row count, attempt count, generation count, sequence-80 row count, admission count, launcher-invocation count, and provider-dispatch count are unchanged (0 deltas); the only seq-80 writer in the candidate is the explicit resume coordinator (`PI_SUBAGENT_RESUME_SEQUENCE`, `piSubagentResumeCoordinator.ts:43`), which no reconnect test imports or reaches.

## T05-AC2 — Restart distinguishes recovered terminal / proven live owner / owner-or-terminal uncertainty

| Aspect | Evidence |
|---|---|
| Source seam | `reconcilePiSubagentExecutions` (`apps/server/src/provider/piSubagentRestartReconciliation.ts:245`), evidence ladder `findLiveOwnerEvidence` (:165) → journal/transcript terminal (:313-365) → `recordOrphanedEvent` (:426); startup composition `makeServerProgram` (`apps/server/src/main.ts:580-636`): `recoverCompletionOutbox` → `runPiSubagentProcessTeardown` (no live owner endpoint) → reconciliation; startup-order test `piSubagentStartupRecoveryOrder.test.ts:116` |
| Normal behavior | `"records no-owner teardown evidence before the Ticket-10 orphan fence"` — order proven: outbox recovery first (recovered 0), band-78 `owner_unproven` journaled while generation still 1 (`proven`/`survivors` rows asserted absent), THEN reconciliation orphans with exactly one generation advance 1→2; a repeated startup pass re-appends nothing (`dispatches` stays `[execution.executionId]` once) (`piSubagentStartupRecoveryOrder.test.ts:116-240`). `"T10-AC3: a live-owner listActive match under the same execution, attempt, and generation refreshes observation without a new attempt"` (`piSubagentRestartReconciliation.test.ts:233`). `"T10-AC2: a transcript terminal marker with matching identity and generation restores the terminal outcome instead of orphaning"` (`:352`). |
| Material failure / diagnostic | `"T10-AC1: no live-owner or terminal evidence produces non-terminal orphaned with a stable owner-loss diagnostic; running is never asserted"` (`piSubagentRestartReconciliation.test.ts:132`) — fixed code `pi_subagent_owner_loss_orphaned` (`piSubagentRestartReconciliation.ts:69`). `"T10-AC6: the orphan diagnostic explains partial side effects and recommends inspection (projected diagnostic contract)"` (`:203`) — fixed message `PI_SUBAGENT_OWNER_LOSS_DIAGNOSTIC_MESSAGE` (`:76-79`) states side effects may exist, recommends inspection before resuming, and states the execution was not automatically replayed. Persistence-failure direction: reconciliation returns `{ kind: "failed" }` entries and emits `pi_subagent_restart_reconciliation_failed` (`piSubagentRestartReconciliation.ts:438-444`), exercised by the repository outage rows in the teardown/outbox files. |
| Fixed code/message | `pi_subagent_owner_loss_orphaned` (+ message above); `pi_subagent_restart_reconciliation_failed`; band values `PI_SUBAGENT_ORPHAN_SEQUENCE = 50` (:66), `PI_SUBAGENT_TERMINAL_SEQUENCE` (terminal coordinator), teardown bands `request/ownerUnproven` asserted in the startup-order test. |
| Durable state effect | Journal-first: `recordOrphanedEvent` / `recordTerminalEvent` in guarded repository transactions; terminal restore is idempotent (`already_applied`), orphan dedup re-reports the already-fenced generation without advancing again (`piSubagentRestartReconciliation.ts:447-467`). `"terminal aggregates are never orphaned (owner loss cannot reverse terminal truth)"` (`test:620`). |
| Attempt/generation effect | Terminal/live-owner outcomes: no new attempt, no fence. Orphan: exactly one generation advance (`generation: execution.generation + 1`, source :476; startup-order test asserts 2 after one orphan and no further advance on replay). |
| Dispatch/launch count | **0 spawn/Resume/dispatch from reconciliation** (asserted by `"T10-AC4: startup reconciliation performs no spawn, resume, or other side-effecting delegation replay"`, `test:310`); startup teardown dispatch occurs only for the handed-off execution and only through the owned supervisor seam (`dispatches === [executionId]`, exactly once across both passes). |
| Bounds/redaction | Terminal restore clamps: `summaryMaxChars` bounds-checked against `MIN/MAX_PI_SUBAGENT_TERMINAL_SUMMARY_MAX_CHARS`, transcriptRef truncated at 1024 chars (`piSubagentRestartReconciliation.ts:203-216`); `"F1: journal→outbox recovery re-clamps unbounded journal-extracted metadata"` (`test:649`). |
| Test result | All pass. Producer exit 0. |

## T05-AC3 — Late old-attempt/generation evidence is fenced, counted, history-only

| Aspect | Evidence |
|---|---|
| Source seam | Repository guarded writes (`recordTerminalEvent`/`recordOrphanedEvent`/`recordTeardownOutcome`/`recordResumeEvent` stale paths, `apps/server/src/persistence/Layers/PiSubagentExecutionRepository.ts:1708-1935, 3093-3241`); `stale_terminal` accounting |
| Normal behavior | `"T07-AC1: first terminal is journaled and applied before notification"`; outbox `"T08-AC3: replayed terminal and replayed outbox processing create no duplicate entry or effect"` (idempotent journal identity). |
| Material failure / diagnostic | `"T07-AC4: stale attempt/generation terminal ignored, counted, truth preserved"` (`piSubagentTerminalLifecycle.test.ts:266`) — stable stale diagnostic, counted, aggregate unchanged. `"T10-AC5: late events from stale attempts or generations after reconciliation are ignored and counted"` (`piSubagentRestartReconciliation.test.ts:437`). `"T14-AC2: late events, terminals, cancels, and completions from the superseded attempt are ignored (generation fence) and counted"` (`piSubagentResumeCoordinator.test.ts:323`). `"T08-AC6: a completion superseded by a newer generation produces no delivery effect; evidence stays readable"` (`piSubagentCompletionOutbox.test.ts:571`). `"recordTeardownOutcome on a stale generation journals history only and never fences the newer attempt"` (`piSubagentProcessTeardown.test.ts:953`). |
| Fixed code/message | `pi_subagent_terminal_stale_ignored` (inherited stable vocabulary; asserted via the stale diagnostic assertions in T07-AC4), plus the Decision-0006 stale-ignored family referenced by the same guards. |
| Durable state effect | Stale evidence is journaled/accounted as history; current aggregate truth (state, outcome, generation) is never mutated by a stale row. `"recordTeardownOutcome(proven) on an already-terminal aggregate journals history only — terminal truth is never reversed"` (`piSubagentProcessTeardown.test.ts:991`). |
| Attempt/generation effect | Stale rows create no attempt, no fence; `"T15-AC6 (F3): a same-generation terminal arriving after the teardown handoff still settles through the normal lifecycle (no premature fence)"` proves fences never precede proof (`piSubagentWatchdogEscalation.test.ts:906`). |
| Dispatch/launch count | **0** — stale handling is pure journal accounting; no launcher, admission, or provider call in any stale row. |
| Bounds/redaction | `"T07-AC5: terminal payload stores bounded summary + transcript reference, never unbounded output"` (`piSubagentTerminalLifecycle.test.ts:327`); `"F1: journal→outbox recovery re-clamps unbounded journal-extracted metadata"` (`piSubagentRestartReconciliation.test.ts:649`). |
| Test result | All pass. Producer exit 0. Stale terminal rows are counted and remain history-only. |

## T05-AC4 — Explicit-only, authorized Resume; same execution/new attempt; provider-inactive denial

| Aspect | Evidence |
|---|---|
| Source seam | Chain: `ProviderCommandReactor.processPiSubagentExecutionResumeRequested` (`ProviderCommandReactor.ts:2891, 2914`, dispatch case `:3707`) → `ProviderService.resumePiSubagentExecution` (`ProviderService.ts:2362-2395`, `allowRecovery: false`) → `PiAdapter.resumePiSubagentExecution` (`PiAdapter.ts:5374-5521`) → `resumePiSubagentExecution` coordinator (`piSubagentResumeCoordinator.ts:159`). |
| Normal behavior | `"T14-AC1: resume keeps the executionId, mints ONE new attemptId, advances generation, and records the new generation BEFORE the child starts"` — outcome `resumed`, same `executionId`, new `att_*`, generation 1→2(orphan)→3(resume), aggregate `queued` on the NEW attempt observable inside `launchChildAttempt` (proves journal-before-child), exactly ONE sequence-80 row (`resumeEvents).toHaveLength(1)`), prior-attempt evidence (seq 1, 2, 50) retained, seq-80 band does not collide with watchdog band 70 (`piSubagentResumeCoordinator.test.ts:202-298`); `"T14-AC5: the resumed card shows the new attempt queued with prior-attempt evidence retained and updated diagnostics"` (`:509`). Success diagnostic `PI_SUBAGENT_RESUME_DIAGNOSTIC_MESSAGE` (`piSubagentResumeCoordinator.ts:45-48`). |
| Material failure / diagnostic (each with zero child/attempt) | Duplicate: `"T14-AC1: replaying the same resume identity is idempotent — NO second attempt, NO second child"` (`:299`). **Structural inactive-runtime row:** ProviderService resolves with `allowRecovery: false`, then emits the exact validation operation/message `Cannot resume subagent execution '<executionId>' because the provider runtime is not active.` at `ProviderService.ts:2362-2366`, before the only adapter invocation at `:2374`; because the coordinator has exactly one production importer/caller through that adapter, adapter/coordinator/sequence-80/attempt/launcher effects are structurally unreachable and no bootstrap is permitted. Unsupported provider: `Provider '<provider>' does not support resuming a managed subagent execution.` Unmanaged/missing-launcher/snapshot: `"T14-AC6/AC3: unmanaged session denies resume — nothing resumes through a legacy bridge"` (`test:538`), adapter denials for launcher/snapshot (`PiAdapter.ts:5388-5405`). Unknown/non-orphaned/stale: `"T14-AC4: an unknown execution denies resume (not_found, no child)"` (`:606`), `"a NON-orphaned execution refuses resume without mutation (invalid_state, no child)"` (`:558`), `stale_generation` outcome (`piSubagentResumeCoordinator.ts:293-298`). Authorization/active-turn/quota: `"T14-AC4: resume re-runs the same admission gates — thread authorization denial creates NO child and NO attempt"` (`:405`), `"missing subject authority fails closed — no child, no attempt"` (`:437`), `"provider-session concurrency quota is enforced — a saturated budget denies resume with no child"` (`:457`) — all via `gate_denied` with the spawn-path diagnostic codes (`piSubagentResumeCoordinator.ts:229-235`). Persistence failure: `pi_subagent_resume_persistence_failed` health degradation, `persistence_failed` outcome, no child (`:254-280`; `"T07-AC1 (failure surface)"` analog in terminal lifecycle). Post-settlement launch failure: `"T14-AC6: a child-start failure leaves the honest durable queued attempt and surfaces child_start_failed"` (`test:624`) — no running/terminal fabrication; the queued attempt is settled honestly by watchdog/wall-time later. |
| Fixed code/message | `pi_subagent_resume_persistence_failed`; `child_start_failed`; `gate_denied` codes; `unmanaged_session` (`pi_subagent_bridge_absent` fallback); `not_found` / `invalid_state` / `stale_generation` / `already_applied`; the ProviderService inactive-runtime sentence above, quoted verbatim from the measured source. |
| Durable state effect | Journal-first `recordResumeEvent` (queued, generation advanced) commits before launch; failures never mutate truth. |
| Attempt/generation effect | Exactly one new attemptId + generation advance per successful explicit resume; all denials: zero attempts. |
| Dispatch/launch count | Success: exactly one `launchChildAttempt` (captured launcher; replay = 0). Every denial class: 0 launches, 0 provider dispatches, 0 admissions. |
| Bounds/redaction | Resume diagnostic carries bounded public identity/lifecycle data only (executionId, attempt, generation, fixed message); prompt content is never re-exposed — cards carry `lastProgressSummary` bounded at 512. |
| Test result | All pass. Producer exit 0. |

## T05-AC5 — Crash diagnostics identify stage and evidence gap without invention

| Aspect | Evidence (minimum rows required by WP §Crash diagnostic matrix; dynamic cases executed in the invocation, with the provider-inactive row proven structurally as labeled above) |
|---|---|
| Terminal ingest/persistence/outbox delivery | `"T08-AC1 (failure direction): outbox write failure rolls back the whole terminal transaction — never terminal without outbox"` (`piSubagentCompletionOutbox.test.ts:193`); `"T07-AC1 (failure surface): terminal persistence failure never notifies and degrades control health"` (`piSubagentTerminalLifecycle.test.ts:489`); `"T08-AC4: crash between terminal persistence and delivery leaves the execution terminal and the outbox recoverably pending"` (`:390`); `"T08-AC4 (journal-first recovery): a terminal journal row without an outbox entry recovers one"` (`:427`). Durable evidence: terminal row committed atomically; missing evidence: delivery entry absent → recoverable pending, never invented. |
| Startup terminal recovery and stale terminal marker | `"T10-AC2"` restore via journal or exact-match transcript marker; mismatched marker restores nothing (`piSubagentRestartReconciliation.test.ts:352` identity+generation guard, source `:347-353`); `"F2: recovery excludes inapplicable stale terminals (no transiently-pending stale entries)"` (`:691`). |
| Exact live-owner refresh and owner/terminal absence | `"T10-AC3"` live-owner refresh (same tuple only — `matchesCurrentAttempt` source `:156-163`); absence of both → `"T10-AC1"` orphaned with `pi_subagent_owner_loss_orphaned` (`test:132`). |
| Late old-generation terminal | `"T10-AC5"` (`test:437`) + `"T07-AC4"` (`piSubagentTerminalLifecycle.test.ts:266`): ignored, counted, truth preserved. |
| Watchdog stage timeout and cleanup uncertainty | `"T15-AC1"` wall-time expiry → child abort with configured stage timeout; `"T15-AC2"` missing child ack → provider-turn interrupt without claiming stopped/cancelled; `"T15-AC5"` timer-only progression never claims stopped or cancelled — projection stays `cancelling` through every stage (`piSubagentWatchdogEscalation.test.ts:187, 253, 472`); `"stage records carry per-stage diagnostic codes (trigger entry, timeout, stopped, cleanup-uncertain)"` (`:1070`); `"T15-AC6"` session-stop timeout → stable diagnostic + band-74 handoff (`:549`). Missing evidence: `"journal stage-record persistence failures surface a stable diagnostic and never wedge the escalation chain"` (`:1002`). |
| Teardown requested / survivors / owner unproven / proven | `"T16-AC2"` request journaled once before dispatch; `"T16-AC3"` only liveness-verified dispatch settles — survivors never settle (`piSubagentProcessTeardown.test.ts:270`); survivor PIDs capped before journaling (`:317`); `"T16-AC4 / Decision 0029"` AggregateError → band 77 + operator diagnostic (`:347`) and missing PID evidence → `unavailable`, never zero survivors (`:405`); `"Decision 0033 §6"` thrown dispatch → non-terminal band 78 `owner_unproven`, never synthetic survivors or a fence (`:837`); `"T16-AC7"` restart with no live supervisor dispatches nothing, kills nothing, records bounded `owner_unproven` once (`:703`); `"T16-AC5"` proven fences + settles cancelled, late same-generation terminals ignored and counted (`:427`). |
| Provider-inactive Resume | ProviderService `!routed.isActive` validation denial — the crash stage "provider runtime unavailable" is reported truthfully; no adapter/coordinator/bootstrap effect (AC4 row). |
| Resume persistence / child-launch failure | `pi_subagent_resume_persistence_failed` (degraded health, honest nonterminal state) and `child_start_failed` (durable `queued` retained) — AC4 row; no synthetic terminal. |
| Bounded execution-card projection | `"T11-AC1: thread-detail snapshot exposes the bounded card aggregate without prompt or raw progress JSON"` (`piSubagentExecutionCardSurface.test.ts:864`); `"T15-AC5 (projection): timeout-only watchdog progression never surfaces a stopped/cancelled claim"` (`:1034`); `"T11-AC3"` card events ride the thread-detail stream so replay-window gaps hit the resync machinery (`:1428`). |
| Forbidden-claim guard (all rows) | `"T15-AC5"` projection row and `"T16-AC3/AC4"` rows assert no `stopped`/`cancelled`/zero-survivor claims from uncertainty; orphan diagnostic asserts "not automatically replayed". |
| Bounds/redaction | `PI_SUBAGENT_EXECUTION_CARD_PROGRESS_SUMMARY_MAX_CHARS = 512` and `PI_SUBAGENT_EXECUTION_CARD_DIAGNOSTIC_MAX_CHARS = 512` (`packages/contracts/src/piSubagents.ts:505-506`); metadata limited to public identity/lifecycle fields; excluded everywhere: raw prompts, `progressJson`, full transcript/result content, provider-local `agentId`, PIDs except capped survivor evidence, commands/secrets (see redaction row below). |
| Test result | All pass. Producer exit 0. |

### Bounds and redaction verification row (required matrix)

| Check | Evidence |
|---|---|
| Cap 64 cards / oldest-overflow omission | `"T11-AC1: terminal summary + delivery state join into the card; cap drops oldest"` — admits `PI_SUBAGENT_EXECUTION_CARD_MAX_PER_THREAD + 5`, asserts `cards).toHaveLength(PI_SUBAGENT_EXECUTION_CARD_MAX_PER_THREAD)` and the OLDEST id absent / NEWEST present (`piSubagentExecutionCardSurface.test.ts:1117-1140`). |
| Progress/diagnostic cap 512 | Contract constants 512/512 (`packages/contracts/src/piSubagents.ts:505-506`); bounded summary/dropped-count fields asserted (`card.lastProgressSummary`, `card.droppedProgressCount` at `test:900-903`). |
| Prompt / raw progressJson / full transcript / result / agentId / PID exclusion | `"T11-AC1"`: seeded `prompt: "SECRET PROMPT CONTENT THAT MUST NEVER LEAVE THE SERVER"` + `progressJson` → serialized snapshot `not.toContain("SECRET PROMPT CONTENT")`, `not.toContain("progressJson")` (`test:929-932`); `"T12-AC5: full result/transcript content never enters lifecycle events, snapshots, or the WS push payload"` — `not.toContain("SECRET T12 PROMPT")`, `not.toContain("entries")`, `not.toContain("resultContent")`, and `Object.hasOwn(...)` false for `entries`/`resultContent`/`transcriptContent` (`test:1447-1500`). `agentId` never appears in card fields; PIDs appear only inside capped survivor evidence (`T16-AC3` cap row). |
| Failure direction of redaction | The redaction assertions ARE the failure surface: any leak flips them from pass to fail; the T11-AC1/T12-AC5 tests were executed green in the producer run. |

## Required Resume matrix → test-case index (all pass)

| Required row | Named case (file) |
|---|---|
| active managed + orphan + gates pass → same executionId, 1 new attempt/generation, 1 seq-80 before 1 launcher | "T14-AC1: resume keeps the executionId, mints ONE new attemptId…" (piSubagentResumeCoordinator.test.ts:202) |
| duplicate explicit command → no second attempt/child | ":299 replaying the same resume identity is idempotent" |
| provider inactive → exact operation/message, zero adapter/coordinator/seq-80/attempt/launcher, no bootstrap | Structural source/caller proof: `ProviderService.resumePiSubagentExecution` resolves with `allowRecovery: false` at `ProviderService.ts:2357-2361`; `!routed.isActive` denial at `:2362-2366` precedes the sole adapter call at `:2374`, and the coordinator is reachable only through that adapter |
| unsupported/unmanaged/missing launcher/snapshot/thread | ":538 T14-AC6/AC3 unmanaged session denies resume"; PiAdapter.ts:5388-5417 (managed/launcher/snapshot denials); ":437 missing subject authority fails closed" |
| unknown/non-orphaned/stale aggregate → bounded denial, zero stale launch | ":606 not_found", ":558 invalid_state", coordinator `stale_generation` |
| authorization/active-turn/quota denial → zero attempt/child | ":405", ":437", ":457" (gate_denied family) |
| persistence failure → `pi_subagent_resume_persistence_failed`, zero child | coordinator :254-280 + health degradation |
| post-settlement child-launch failure → honest durable queued attempt | ":624 T14-AC6 child-start failure" |
| success → `pi_subagent_resumed` semantics, prior-attempt evidence preserved | ":202" (seq 1/2/50 retained; seq-80 unique band) + ":509 T14-AC5 card row" |

## Required reconnect/snapshot/cursor matrix → covered in T05-AC1 row

Durable card hydration with no in-memory agent row (cards read from repository only — `getExecutionCard` fixture seam), fresh subscriber/client over the same isolated durable state (`wsSnapshotLiveStream` independent streams + per-key escalation tracker `:355`), cap 64 + oldest omission, 512 caps, exclusion list, exact-gap replay without snapshot, excessive-gap/ahead/deleted-subject/unbounded-attach resnapshot, and zero work-creation counters — each mapped above.

**Matrix provenance:** every named case quoted here exists in the frozen candidate's test files and was executed by the single producer invocation (9 files, 118 tests, exit 0). Candidate SHA `7521b92c7cb8a614346f994e963aa379175f540b`. No source, test, or configuration seam was modified.
