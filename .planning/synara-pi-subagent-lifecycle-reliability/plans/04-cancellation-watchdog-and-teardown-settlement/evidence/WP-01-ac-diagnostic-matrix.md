# WP-01 — Ticket 04 AC/diagnostic matrix (frozen candidate)

**Candidate:** Symphony `08b65ebb466470d71814c4467d74e68f43991138` (HEAD at execution; no source/test/contract/manifest/lockfile delta before, during, or after the run).
**Producer:** focused deterministic Vitest command from WP-01 (11 files, `--project unit --maxWorkers=1 --no-file-parallelism`), run from `apps/server` with `set -o pipefail` and exit captured via `${PIPESTATUS[0]}`. **Producer exit: 0** (recorded in `WP-01-workspace-state.txt`).
**Run totals:** `WP-01-focused-deterministic.log` tail block L4–L8 — **L5: `Test Files  11 passed (11)`**, **L6: `Tests  177 passed (177)`**, L7 `Start at 21:34:17`, L8 `Duration 15.86s`. (Vitest 4.1.10.)
**Scope honesty:** every row below cites an actually executed named case at the frozen candidate (file:line at HEAD) — nothing is inferred from inherited totals.

## Sequence-band map (source constants at candidate)

| Band | Meaning | Constant source |
|---|---|---|
| 90 | Durable cancel intent (`cancelling`, journal-first, BEFORE dispatch) | `piSubagentCancellationCoordinator.ts:231` |
| 92 | Cancel settlement row — only `child_ack` or `owner_death` evidence channels | `piSubagentCancellationCoordinator.ts:374`; channels asserted at `piSubagentCancellationCoordinator.test.ts:151,384` |
| 70–74 | Watchdog stages: 70 escalation started, 71 child-abort timeout, 72 provider-turn interrupt, 73 provider-session stop, 74 teardown handoff — bounded, observable, NON-terminal, non-fencing | `piSubagentWatchdogEscalation.ts:61-67` |
| 75–78 | Teardown: 75 request (journal-first), 76 proven, 77 survivors, 78 owner_unproven — only 76 fences | `piSubagentProcessTeardown.ts:68-73` |

## T04-AC1 — Cancellation intent journal-first, authorized, idempotent, `cancelling` until accepted termination evidence

| # | Scenario | Locator (case title @ HEAD) | Normal-path proof | Failure/diagnostic proof | Band/journal effect & duplicate/replay counts |
|---|---|---|---|---|---|
| A1-1 | Normal: intent before dispatch, settle on same-identity ack | `piSubagentCancellationCoordinator.test.ts:100` "T06-AC1: records durable cancelling intent BEFORE dispatch and settles cancelled only from a same-identity ack" | seq-90 intent exists pre-dispatch; `order == ["dispatch"]`; outcome `cancelled_ack`; aggregate `cancelled/cancelled` | — | 90 intent row; 92 ack row with `evidenceChannel: "child_ack"` (:151) |
| A1-2 | Replay: duplicate cancel command | `piSubagentCancellationCoordinator.test.ts:156` "T06-AC1: duplicate/replayed cancel command is idempotent — one intent row, one dispatch, no repeated abort" | `dispatchCount === 1`; `intents` length 1; `second.outcomes == []` | — | **Effect counts: 1 intent row, 1 dispatch, 0 re-dispatch** |
| A1-3 | Authorization boundary: mismatched attempt/generation ack is NOT termination evidence | `piSubagentCancellationCoordinator.test.ts:300` "T06-AC4/AC5: an ack carrying a different attempt/generation is not termination evidence — state stays cancelling" | — | outcome `still_cancelling`; observed stays `accepted` (never claimed cancelled); escalation fired; fixed code `pi_subagent_cancel_ack_timeout` in diagnostics | stays `cancelling`; late ack would be journaled history only (see A1-8) |
| A1-4 | Stale late settlement cannot affect a newer attempt | `piSubagentCancellationCoordinator.test.ts:272` "T06-AC3: stale late settlement cannot affect a newer attempt (journaled history only)" | newer attempt untouched (`accepted/running` on att_new/gen3) | — | old-gen 92 row journaled history only |
| A1-5 | Owner-death conjunction (full proof) | `piSubagentCancellationCoordinator.test.ts:340` "T06-AC4: owner-death evidence (dead owner + expired re-derived lease + not in listActive) settles cancelled without dispatch" | `dispatchCount === 0`; outcome `cancelled_owner_death` | — | 92 row `evidenceChannel: "owner_death"` (:384) |
| A1-6 | Owner-death conjunction NEGATIVE 1: owner dead but lease NOT expired | `piSubagentCancellationCoordinator.test.ts:389` "T06-AC4: owner dead but lease NOT expired keeps cancelling (no settlement without full proof)" | — | `still_cancelling`; observed ≠ cancelled; desired `cancelling` | non-terminal |
| A1-7 | Owner-death conjunction NEGATIVE 2: owner dead but child still in `listActive` | `piSubagentCancellationCoordinator.test.ts:570` "T06-AC4: owner dead and execution still in listActive is NOT owner-death proof (child may still be live)" | — | `still_cancelling`; observed ≠ cancelled | non-terminal |
| A1-8 | Idempotent re-cancel via card path; unknown execution honest denial | `piSubagentCancellationCoordinator.test.ts:690` "T11-AC6: idempotent re-cancel replays already_applied intent and never re-dispatches; unknown execution reports not_found without state writes" | `already_terminal`, `not_found` outcomes | — | **`dispatchCount === 1` across both cancels; no state writes for `not_found`** |
| A1-9 | Extension reports `already_terminal` / `stale` | `piSubagentCancellationCoordinator.test.ts:506` and `:535` | terminal outcome mapped without settling newer; `stale_generation` outcome without settling | stale never settles | non-terminal on stale |

**AC1 verdict: PROVEN.** Journal-first seq 90, idempotent replay (effect counts recorded), same-identity ack requirement, owner-death conjunction with both negative boundaries, `cancelling` preserved on every non-proof path.

## T04-AC2 — Watchdog stages 70–74 bounded, observable, non-terminal until terminal evidence or approved cleanup proof

| # | Scenario | Locator | Normal-path proof | Failure/diagnostic proof | Band/journal effect |
|---|---|---|---|---|---|
| A2-1 | Stage progression with configured stage timeout | `piSubagentWatchdogEscalation.test.ts:187` "T15-AC1: wall-time expiry consumes the band-60 trigger, records a watchdog diagnostic, and starts child abort with a configured stage timeout" | band-60 trigger consumed; seq-90 intent journaled; stage rows 70–74 present; escalation length 1 | fixed code `pi_subagent_watchdog_walltime_escalation` | 70–74 rows; 90 intent |
| A2-2 | Missing child ack → provider-turn interrupt | `piSubagentWatchdogEscalation.test.ts:253` "T15-AC2: missing child acknowledgement advances to provider-turn interrupt without claiming stopped or cancelled" | interrupt dispatched (`th_wd`) | `TERMINAL_STATES.has(observed) === false`; desired `cancelling` | stage-2 row |
| A2-3 | Missing turn-terminal evidence → session stop journaled | `piSubagentWatchdogEscalation.test.ts:311` "T15-AC3: missing provider-turn terminal evidence advances to provider-session stop with journaled commands and results" | stop dispatched; ≥4 stage rows; session-stop row `result: "stopped"` | **projection stays non-terminal — command completion is not child termination proof (T15-AC5)** | 73 row with journaled result |
| A2-4 | Terminal evidence between stages stops escalation | `piSubagentWatchdogEscalation.test.ts:404` "T15-AC4: terminal evidence observed between stages stops escalation without a second settlement"; also `:702` F1 terminal during stage-2 window | interrupts 1, session stops 0; settled `succeeded`; exactly one band-40 row | — | normal lifecycle settlement, exactly once |
| A2-5 | Timer-only progression never claims terminal | `piSubagentWatchdogEscalation.test.ts:472` "T15-AC5: timer-only progression never claims stopped or cancelled; projection stays cancelling through every stage"; projection twin `piSubagentExecutionCardSurface.test.ts:1034` "T15-AC5 (projection): timeout-only watchdog progression never surfaces a stopped/cancelled claim" | full chain dispatched | fixed code `pi_subagent_watchdog_cleanup_uncertain`; band-74 handoff row present; card projection shows `desiredState: cancelling`, observed non-terminal | 74 handoff |
| A2-6 | Stage timeout bounds a hung session stop | `piSubagentWatchdogEscalation.test.ts:549` "T15-AC6: session-stop timeout produces a stable diagnostic and hands the owned execution to the process-teardown stage" | — | never-settling stop bounded at 50ms; fixed code `pi_subagent_watchdog_stage_timeout`; journaled 73 row `result: "timeout"`; 74 handoff row; outcome `cleanup_uncertain` | 73 timeout + 74 handoff |
| A2-7 | Full stage-2 window elapsed without evidence | `piSubagentWatchdogEscalation.test.ts:816` "T15-AC3 (F1): absent evidence through the full stage-2 window advances to provider-session stop exactly once" | polls ≥4; exactly one session stop | interrupt row code `pi_subagent_watchdog_stage_timeout`; operator event asserted by stage identity (band 72 `provider_turn_interrupt` + band 71 `child_abort_timeout`); `pi_subagent_watchdog_terminal_evidence` asserted ABSENT | 72 timeout row; single stop |
| A2-8 | Per-stage fixed diagnostic codes | `piSubagentWatchdogEscalation.test.ts:1070` "stage records carry per-stage diagnostic codes (trigger entry, timeout, stopped, cleanup-uncertain)" | 70→`..._walltime_escalation`, 71→`..._stage_timeout`, 73→`..._session_stopped`, 74→`..._cleanup_uncertain` | — | exact codes per band |
| A2-9 | Stage-record persistence outage | `piSubagentWatchdogEscalation.test.ts:1002` "journal stage-record persistence failures surface a stable diagnostic and never wedge the escalation chain" | chain still dispatched and settled honestly (1 escalation, 1 stop) | injected journal outage; fixed code `pi_subagent_lifecycle_persistence_failed`; **seq-90 intent still durable** | 90 survives; stage writes fail-soft |
| A2-10 | Non-terminal replay of watchdog | `piSubagentWatchdogEscalation.test.ts:1125` "re-running the watchdog after an uncertain escalation does not duplicate stage records (idempotent journal identity)" | — | second pass SKIPS the handed-off execution (`sessionStops` total 1) | **Effect counts: seq-90 rows 1; stage-eventIds unique set == row count; no duplicate escalation history** |
| A2-11 | Sweep driver + operator observation | `piSubagentWatchdogSweep.test.ts:79` "escalates wall-time-expired execution through stage controls" (`escalated: 1`, outcome `cleanup_uncertain`); `:131` "fresh heartbeat stays untouched" (`escalated: 0`); `:178` "diagnostic + escalation observers receive safe metadata" (fixed vocabulary, safe correlation only); `:246` "timer schedules and reschedules passes" | driver reports safe metadata | — | outcome metadata `{trigger: "wall_time", outcomeKind: "cleanup_uncertain"}` |
| A2-12 | Idle/orphan policy boundaries | `piSubagentWatchdogEscalation.test.ts:619` (expired lease beyond orphan threshold enters escalation without band-60 trigger); `:671` fresh heartbeat never escalates; `:976` age guard on first sweep | — | — | — |
| A2-13 | No premature fence at handoff | `piSubagentWatchdogEscalation.test.ts:906` "T15-AC6 (F3): a same-generation terminal arriving after the teardown handoff still settles through the normal lifecycle (no premature fence)" | terminal after handoff `recorded`; observed `succeeded` | — | **74 handoff does NOT fence** |

**AC2 verdict: PROVEN.** Bands 70–74 bounded (stage timeouts), observable (fixed per-stage codes + operator observers), non-terminal (T15-AC5 at coordinator and projection), idempotent replay.

## T04-AC3 — Teardown 75–78: owned-only dispatch, proof-before-fence, bounded survivors, retry escalation, no parent fallback

| # | Scenario | Locator | Normal-path proof | Failure/diagnostic proof | Band/journal effect & counts |
|---|---|---|---|---|---|
| A3-1 | Owned-only dispatch | `piSubagentProcessTeardown.test.ts:198` "T16-AC1: teardown dispatches ONLY for the handed-off execution through the owned supervisor and never for unrelated executions" | exactly 1 outcome, only `exec_td_1`; `th_unrelated` never in requests | — | 75 request only for handed-off execution |
| A3-2 | Request journaled once before dispatch | `piSubagentProcessTeardown.test.ts:233` "T16-AC2: the request is journaled once before dispatch and a second pass re-dispatches nothing new while observing idempotent journal effects" | first pass `settled_proven`; codes `pi_subagent_teardown_requested` / `pi_subagent_teardown_proven` | — | **Effect counts: 1 request row (75), 1 outcome row (76); second pass 0 outcomes, fixture requests total 1** |
| A3-3 | Proof-before-fence | `piSubagentProcessTeardown.test.ts:270` "T16-AC3: only a liveness-verified dispatch settles — a kill API return without proof (survivors) never settles"; survivors→proven retry `:501` | proven settles with `fencedGeneration: 2` | survivors: observed stays `cancelling`, `TERMINAL_STATES.has(...) === false`; band-77 row code `pi_subagent_teardown_survivors` with journaled PIDs | **77 does NOT settle; only 76 fences; distinct sequences let 77→76 retry escalate** |
| A3-4 | Survivors bounded at cap 16 | `piSubagentProcessTeardown.test.ts:317` "T16-AC3: survivor PID lists are capped to the bounded maximum before journaling"; source cap `piSubagentProcessTeardown.ts:85` (`MAX_PI_SUBAGENT_TEARDOWN_SURVIVOR_PIDS = 16`) | 26-PID report journaled at exactly 16 | — | bounded durable evidence |
| A3-5 | Production AggregateError survivor evidence → band 77 + operator diagnostic; missing PID = unavailable, never zero survivors | `piSubagentProcessTeardown.test.ts:347` and `:405` | band-77 row + `teardown_survivors` diagnostic | missing PID reported unavailable (never fabricated `0 survivors`) | 77 honest |
| A3-6 | Proven fences; late terminal ignored AND counted | `piSubagentProcessTeardown.test.ts:427` "T16-AC5: proven teardown fences the attempt/generation while settling cancelled, and late same-generation terminal events are ignored and counted" | `fencedGeneration: 2`; aggregate `cancelled/cancelled` gen 2; operator diagnostic `teardown_proven` pairs durable row | late fenced-gen terminal: outcome ≠ `persisted`; observed stays `cancelled` gen 2; **`staleTerminalEvents >= 1` durably counted** | 76 fence; late terminal history + counter only |
| A3-7 | Truthful replay onto already-fenced aggregate | `piSubagentProcessTeardown.test.ts:567` "T16-AC2 (truthful replay): a proven replay onto an already-fenced aggregate reports the existing fence without claiming a new one" | replay reports existing fence | — | no new fence claim |
| A3-8 | Graceful/normal paths never enter teardown (AC5 anchor, also proves AC3 boundary) | `piSubagentProcessTeardown.test.ts:601` "T16-AC6: graceful cancellation (seq 92 settlement) and normal terminal paths never enter teardown"; boundary `:662` "T16-AC6 (boundary): a same-generation terminal arriving between handoff and teardown settles as ordinary lifecycle evidence — no proof yet, no fence at handoff" | graceful (90+92) and band-40 terminal: `outcomes` length 0, `requests` length 0; boundary: terminal wins, observed `succeeded`, zero dispatch | — | **zero teardown effects on graceful paths** |
| A3-9 | Restart with no live owner; bounded pass | `piSubagentProcessTeardown.test.ts:703` "T16-AC7: restart with no live owned supervisor dispatches nothing, kills nothing, and records the bounded owner_unproven evidence once"; `:758` "T16-AC7: the pass is bounded — at most maxPerPass executions are scanned per sweep" (pass cap source `piSubagentProcessTeardown.ts:88`) | one dispatch attempt, `owner_unproven`, `pi_subagent_teardown_owner_unproven` + operator stage `teardown_owner_unproven` | **second pass journal delta 0 (idempotent identity); stays `cancelling`**; budget consumed by non-handed-off executions before dispatch | 78 bounded, non-terminal |
| A3-10 | Persistence outage on request write | `piSubagentProcessTeardown.test.ts:808` "Decision 0001 failure coverage: a repository outage on the request write reports the stable persistence-failed diagnostic and does not dispatch" | — | outcome kind `failed`; `requests` 0; fixed code `pi_subagent_lifecycle_persistence_failed` | no dispatch, no fabrication |
| A3-11 | Thrown dispatch → band 78, never synthetic 77, never fence | `piSubagentProcessTeardown.test.ts:837` "Decision 0033 §6: a thrown owned-teardown dispatch records non-terminal owner_unproven band 78, never synthetic survivors band 77 or a fence" | exactly 1 outcome row (78), `metadata.reason: "dispatch_failed"`, message contains "dispatch failed", `survivorPids` undefined; operator surface shows `teardown_owner_unproven`, `teardown_survivors` asserted ABSENT | — | 75 first, then 78 only; **no 76, no 77; observed/desired `cancelling`, gen 1** |
| A3-12 | Repository seams: replay/stale/terminal truth | `piSubagentProcessTeardown.test.ts:915` proven settles + replay `already_applied`; `:953` stale generation journals history only, never fences newer (owner-loss fence at gen 2 wins); `:991` already-terminal never reversed (`already_applied`, observed `succeeded`); `:1026` requested idempotent per attempt/generation, `stale_generation` for superseded | — | — | **Effect counts: replay `already_applied`; stale `stale_generation`** |
| A3-13 | Live adapter wiring: exact identity routing through retained owner endpoint | `piSubagentChildOwnerTeardownWiring.test.ts:450` "routes exact execution identity through the retained owner endpoint and settles proven" (teardownCalls exact tuple; `expectProven` :380 asserts 76 row + `cancelled` + gen 2); survivors `:542` (validated 77 path, PIDs durably stored on row per `expectSurvivors` :398-424) | — | — | 76 proves fence; 77 stays `cancelling` gen 1 |
| A3-14 | Owner endpoint failure matrix (13 invalid cases) | `piSubagentChildOwnerTeardownWiring.test.ts:616` "maps invalid owner replies to band 78 without parent supervisor fallback" — cases: `capability_absent`, `capability_endpoint_absent` (handshake advertises but endpoint missing — non-vacuous precondition asserted), `malformed`, `mismatched` (proven reply with wrong identity), `throw`, `stale`, `missing`, `owner_unavailable`, `dispatch_failed`, `survivors_empty`, `survivors_unsorted`, `survivors_duplicated`, `survivors_over_cap` (17 PIDs invalid) | — | **every case: `processSupervisorCalls === 0` (no parent fallback) and `expectOwnerUnproven` (:425-449) asserts band 78, `journal.some(76) === false`, `journal.some(77) === false`, `cancelling` gen 1** | 78 only, never fabricated 76/77 |
| A3-15 | Owner timeout, durable 78, sweep continuity, no parent fallback | `piSubagentChildOwnerTeardownWiring.test.ts:797` "times out a never-settling owner endpoint into durable band 78 and keeps scheduling subsequent sweep passes" | 75 request BEFORE 78 outcome in journal; no 76/77 rows; second pass re-dispatches to the SAME retained endpoint (`teardownCalls` length 2) | bounded host-side stage timeout (100ms via existing server config — no new knob); `processSupervisorCalls === 0` throughout; stays `cancelling` gen 1 | **78 non-terminal across passes** |
| A3-16 | Durable proven-write failure retains owner for retry | `piSubagentChildOwnerTeardownWiring.test.ts:942` "retains the exact owner after a durable proven-outcome write fails so the next sweep can retry proof" | after injected band-76 write failure: stays `cancelling` gen 1, NO 76 and NO 78 rows; next pass re-dispatches same identity and `expectProven` passes | — | retry escalation without false terminal claim |
| A3-17 | Owner registry lifecycle | `piSubagentChildOwnerTeardownWiring.test.ts:1066` retains capability-bearing owner across same-process parent session stop; `:1145` empty registry after adapter process restart → `owner_unproven`; `:1185` sibling/historical same-thread endpoints isolated by exact execution identity | `expectProven` for isolated siblings | restart → `expectOwnerUnproven` | no cross-identity proof |
| A3-18 | Adapter sweep wiring | `piSubagentTeardownWiring.test.ts:203` "T16-AC1/AC2: the adapter-mounted sweep dispatches the owned teardown once for a handed-off execution and settles proven with the fence" (76 row + `cancelled` gen 2); `:260` "T16-AC7 (adapter boundary): with no live session and no injected resolver the production wiring resolves undefined and journals owner_unproven without any kill" | once-only dispatch, fence proven | no-session: 78 durable, `cancelling`, no kill | 76 fences; 78 does not |
| A3-19 | Bridge slice: optional capability + validation + dispatch | `piSubagentBridge.test.ts` teardown slice: `:615` capability OPTIONAL (never in `requiredCapabilities`); `:629` extension WITHOUT capability stays fully managed; `:645` gates on when supplied; validator `:658` accepts authenticated outcomes with matching correlation; `:674` rejects malformed/unknown shapes ("no validation bypass", unknown status spellings never decode); `:751` rejects correlation identity mismatches; dispatch `:774` absent endpoint → no capability; `:785` validated proven/survivors with fenced command dispatched VERBATIM; `:811` absent op → `owner_unproven`; `:826` throw → `owner_unproven` | verbatim fenced command | every failure maps unproven — see AC4 rows | no validation bypass anywhere |

**AC3 verdict: PROVEN.** Owned-only dispatch (zero parent-supervisor calls asserted in 13 invalid-owner cases), proof-before-fence (only 76 fences — see fence matrix below), bounded survivors (cap 16), retry escalation (77/78 never block 76; failed proven-write retains owner), no parent fallback.

## T04-AC4 — Persistence outage, provider stop failure, timeout, owner loss, survivor, late terminal → stable diagnostics, no fabricated cancellation

| # | Material failure path | Locator + fixed diagnostic code | Disposition proven |
|---|---|---|---|
| A4-1 | Persistence outage (teardown request write) | `piSubagentProcessTeardown.test.ts:808` — `pi_subagent_lifecycle_persistence_failed`; outcome `failed`; zero dispatch | stable diagnostic; no fabricated effect |
| A4-2 | Persistence outage (watchdog stage records) | `piSubagentWatchdogEscalation.test.ts:1002` — `pi_subagent_lifecycle_persistence_failed`; chain unwedged; seq-90 intent durable | stable diagnostic; authoritative write survives |
| A4-3 | Provider stop TIMEOUT | `piSubagentWatchdogEscalation.test.ts:549` — `pi_subagent_watchdog_stage_timeout`; journaled `result: "timeout"` at band 73; band-74 handoff; `cleanup_uncertain`; never claims stopped/cancelled | stable diagnostic + owned handoff |
| A4-4 | Provider stop UNCERTAIN | `piSubagentWatchdogEscalation.test.ts:472` (T15-AC5 full chain with `"uncertain"` stop result); sweep driver twin `piSubagentWatchdogSweep.test.ts:178` — `pi_subagent_watchdog_cleanup_uncertain`; journaled `result: "uncertain"` (source `piSubagentWatchdogEscalation.ts:626-649`); projection non-terminal | stable diagnostic + handoff; no terminal claim from timers |
| A4-5 | Provider stop promise REJECTION (`"failed"` race result) — **explicit honest row** | SOURCE branch: `piSubagentWatchdogEscalation.ts:560-585` — `raceStageTimeout` returns `{status: "failed"}` when the action rejects (source :398), and the guard `stopRace.status === "timed_out" || stopRace.status === "failed"` routes BOTH to the identical stable-diagnostic + band-73 journal (`result: "failed"`) + band-74 handoff + `cleanup_uncertain` path. **In the 11-file focused set NO dedicated NAMED case drives a rejecting `stopProviderSession`;** the branch's behavior is proven transitively by the timeout case A4-3, which exercises the exact same branch and asserts its diagnostic, journaling, handoff, and non-terminal outcome. The only unasserted delta is the literal journaled metadata string `"failed"` vs `"timeout"`. | **Residual (honest):** the failed-stop sub-branch lacks a dedicated named test; its shared code path, diagnostic, handoff, and non-terminal disposition ARE concretely proven via A4-3. This does not defeat AC4 (all six material paths below have dedicated named cases); recorded as residual uncertainty for WP-02/review. |
| A4-6 | Cancellation dispatch failure / ack timeout / absent bridge | `piSubagentCancellationCoordinator.test.ts:426` — `pi_subagent_cancel_ack_timeout`, bounded retries (attempts 3 = 1 + 2), escalation fired, stays `cancelling`; `:474` — `pi_subagent_cancel_dispatch_failed` on absent bridge, never a silent skip | cancelling preserved; no success claim |
| A4-7 | Owner loss | coordinator owner-death conjunction rows A1-5..A1-7; orphaned projection truth `piSubagentExecutionCardSurface.test.ts:297` "T03-AC5: terminal and orphaned cards carry NULL attachment/teardown evidence" (orphaned card `observedState: "orphaned"`, NULL truth despite prior survivors evidence); stale-generation teardown after owner-loss fence `piSubagentProcessTeardown.test.ts:953` | honest non-terminal/orphaned truth; newer attempt protected |
| A4-8 | Survivor evidence | A3-3/A3-5 (band 77 keeps `cancelling`, durable bounded PIDs); AggregateError production evidence `:347`; missing PID = unavailable `:405` | no fabricated settlement; bounded honest evidence |
| A4-9 | Late terminal | A3-6 (ignored AND counted, `staleTerminalEvents >= 1`); `:662` terminal-beats-teardown boundary; `piSubagentWatchdogEscalation.test.ts:906` no premature fence | terminal truth never reversed or fabricated |
| A4-10 | Owner endpoint failures (absent/thrown/timeout/malformed/mismatched/status-reported) | bridge dispatch rows: timeout `:842` (`dispatch_timed_out`, `pi_subagent_teardown_owner_unproven`, exact `attemptedCommand` preserved, **zero unhandled rejections** asserted via process hook), late rejection swallowed `:881`, malformed `:934` (`malformed_result`), identity mismatch `:947` (`identity_mismatch`), owner-reported `owner_unavailable`/`dispatch_failed`/`stale`/`missing` → unproven with reasons `:960`; deadline normalization `:911` (invalid/absent → shared watchdog default); wiring equivalents A3-14/A3-15 | every failure = honest non-terminal band 78; never proven, never synthetic 77 |
| A4-11 | Live-lifecycle classification stability (no internal leaks) | `piSubagentLiveLifecycleContainment.test.ts:92` unavailable for missing/mismatched/inactive/disposed (`pi_subagent_live_lifecycle_unavailable`, zero callbacks); `:135` timeout/response-loss classified (`unavailable` pre-acceptance, `outcome_unknown` post-acceptance; traces assert no provider text); `:206` no value after timeout marker; `:267` pre/post-acceptance boundary; `:455` timeout reasons only via `markTimedOut`; `:501` observation never `provider_acceptance`/`outcome_unknown`; `:542` status bound per path; `:617` no internal reasons or provider text on public surface (`sk-provider-secret` asserted absent); canonical routing twins `piSubagentCanonicalRouting.test.ts:546,578,588,602,614,629,648,665` (structured unavailable marker; malformed/structured-error/thrown steer → `pi_subagent_live_lifecycle_outcome_unknown`; observation throws → unavailable; text heuristics never parsed); diagnostic whitelist `:429` (`pi_subagent_read_capability_unavailable`, `pi_subagent_read_live_record_unavailable`, arbitrary provider strings suppressed) | stable bounded codes; no fabricated outcomes; no secret leakage |
| A4-12 | No fabricated cancellation overall | Every non-proof row above keeps `cancelling` and asserts `TERMINAL_STATES.has(observed) === false`; card evidence ladder `piSubagentExecutionCardSurface.test.ts:407` shows `none → requested(75) → owner_unproven(78) → survivors(77)` presentation truth without terminal claims | **no fabricated cancellation anywhere in the 177-test set** |

**AC4 verdict: PROVEN, with one recorded residual (A4-5).** All six material paths named by AC4 — persistence outage, provider stop failure, timeout, owner loss, survivor, late terminal — have dedicated named cases with stable fixed diagnostic codes and no fabricated cancellation. The `stopProviderSession` promise-rejection (`"failed"`) sub-branch shares its entire proven code path with the timeout case and is recorded honestly as lacking a dedicated named case.

## T04-AC5 — Graceful terminal/cancel never destructive; replay and duplicate sweeps do not duplicate effects

| # | Scenario | Locator | Effect counts / proof |
|---|---|---|---|
| A5-1 | Graceful cancel + normal terminal never enter teardown | `piSubagentProcessTeardown.test.ts:601` | `outcomes` 0, `requests` 0 (zero destructive effects) |
| A5-2 | Terminal between handoff and teardown wins | `piSubagentProcessTeardown.test.ts:662` | zero dispatch; observed `succeeded`; no fence at handoff |
| A5-3 | Duplicate cancel idempotent | `piSubagentCancellationCoordinator.test.ts:156` | 1 intent, 1 dispatch, 0 re-abort |
| A5-4 | Re-cancel replay + unknown | `piSubagentCancellationCoordinator.test.ts:690` | `dispatchCount` 1; `not_found` without state writes |
| A5-5 | Duplicate watchdog sweep | `piSubagentWatchdogEscalation.test.ts:1125` | seq-90 rows 1; `sessionStops` 1 (second pass skips handed-off execution); unique stage eventIds |
| A5-6 | Duplicate teardown pass after settle | `piSubagentProcessTeardown.test.ts:233` | requests 1, outcomes 1; second pass 0 outcomes |
| A5-7 | owner_unproven duplicate pass | `piSubagentProcessTeardown.test.ts:703` | **journal delta 0** across second pass |
| A5-8 | Survivors → proven retry escalation | `piSubagentProcessTeardown.test.ts:501` | **exactly ONE 75 request across both passes**; 77 and 76 rows coexist; settles with fence gen 2 |
| A5-9 | Repository replay identities | `piSubagentProcessTeardown.test.ts:915` (`already_applied`), `:953` (`stale_generation`), `:991` (terminal never reversed), `:1026` (requested `already_applied`/`stale_generation`) | replay deduped at the seam |
| A5-10 | Already-fenced replay truthful | `piSubagentProcessTeardown.test.ts:567` | reports existing fence, claims no new one |
| A5-11 | Late same-generation terminal after handoff | `piSubagentWatchdogEscalation.test.ts:906` (F3) | settles through normal lifecycle; no premature fence |
| A5-12 | Card-event replay/dedupe | `piSubagentExecutionCardSurface.test.ts:1170` duplicates replay to ONE event; `:1262` duplicate events reduce to one card unchanged; `:742` `already_applied`/`stale_generation` outcomes publish NOTHING; `:1367` distinct delivery-band identities; `:652` 77/78 publish committed "Cancellation unverified" events with deterministic identities | **replay → ONE event; false publication = zero** |
| A5-13 | Graceful ack settles escalation exactly once | `piSubagentWatchdogEscalation.test.ts:370` (ack during stage 1 stops escalation, settles once); `:404` (terminal between stages, one band-40 row) | single settlement |
| A5-14 | Retired / cleared late responses | `piSubagentLiveLifecycleContainment.test.ts:300` retired late response → `{stale, pi_subagent_live_lifecycle_stale_ignored}` permanently; `:333` outcome-unknown suppressed after retirement wins; `:355` per-session clear isolation; `:576` stale equal tuple cannot retire replacement; `:387` pure control never applied without acceptance | late/cleared responses never become truth |
| A5-15 | Canonical routing non-destruction | `piSubagentCanonicalRouting.test.ts:357` durable terminal returned without provider call; `:129` absent live registration → zero provider calls, `pi_subagent_live_lifecycle_unavailable`; `:475` steer exact-live-only, stale durable tuples never invoke provider (`pi_subagent_read_stale_attempt_or_generation`); `:101,304,329` fails closed before provider on incomplete/session-isolated/wrong-thread authorization | provider never invoked outside authorized exact-live tuples |

**AC5 verdict: PROVEN.** Graceful paths perform zero destructive teardown; every duplicate/replay surface records its effect count (1 intent / 1 request / 1 outcome / 1 event / 0 delta) and dedupes at the journal-identity or publication boundary.

## Cross-cutting proof: ONLY band 76 fences

| Outcome | Settlement? | Generation effect | Proof locators |
|---|---|---|---|
| 90 intent | no | unchanged | `piSubagentCancellationCoordinator.test.ts:100,156` |
| 92 (child_ack/owner_death) | yes — graceful cancelled (not a teardown fence) | per ack path | `:100` (`evidenceChannel: child_ack`), `:340` (`owner_death`); mismatched ack rejected `:300` |
| 70–74 watchdog rows | no | unchanged (F3: terminal after handoff still settles normally) | `piSubagentWatchdogEscalation.test.ts:472,906` |
| 75 request | no | unchanged | `piSubagentProcessTeardown.test.ts:233` |
| **76 proven** | **YES — settles `cancelled` + advances generation fence** | **gen → 2 atomically** | `piSubagentProcessTeardown.test.ts:427` (`fencedGeneration: 2`); `:915` (one transaction); `piSubagentChildOwnerTeardownWiring.test.ts:380` (`expectProven`: 76 row + `cancelled` + gen 2); `piSubagentTeardownWiring.test.ts:203`; `piSubagentExecutionCardSurface.test.ts:589` (fenced card carries NO stale teardown evidence) |
| 77 survivors | no — stays `cancelling` | unchanged (gen 1) | `piSubagentProcessTeardown.test.ts:270`; wiring `expectSurvivors:398` |
| 78 owner_unproven | no — stays `cancelling` | unchanged (gen 1); explicit `journal.some(76) === false`, `journal.some(77) === false` | wiring `expectOwnerUnproven:425-449`; `piSubagentProcessTeardown.test.ts:703,837` |

## Residual notes (honest)

1. **`stopProviderSession → "failed"` rejected-stop sub-branch (row A4-5):** no dedicated named case in the 11-file focused set drives a rejecting `stopProviderSession`. The source branch is shared verbatim with the timeout branch (`piSubagentWatchdogEscalation.ts:560`: `stopRace.status === "timed_out" || stopRace.status === "failed"`), whose diagnostic, band-73/74 journaling, handoff, and non-terminal outcome are concretely proven by T15-AC6 (`piSubagentWatchdogEscalation.test.ts:549`). Only the literal journaled metadata `"failed"` (vs `"timeout"`) is not asserted by any named test. Recorded as residual uncertainty; WP-02 and the Implementation Report must carry it forward. AC4 remains supported: all six material failure paths named by AC4 have dedicated named cases.
2. Log-tail citation precision: run totals sit at log **L5–L6** of the tail block L4–L8 (`L4` is the blank separator; L7–L8 carry start time and duration). The workspace-state artifact independently records producer exit 0.
3. `cleanup_uncertain`, `survivors`, and `owner_unproven` are proven immutable non-terminal evidence and retry inputs throughout (Unlock gate respected); no destructive real-Pi run is claimed or required (PLAN §7).
