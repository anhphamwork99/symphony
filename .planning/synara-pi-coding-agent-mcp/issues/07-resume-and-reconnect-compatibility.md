# Resume and reconnect compatibility

Labels: `wayfinder:grilling`

## Question

How must Synara persist and restore default coding-agent tool configuration,
Synara MCP activation, MCP discovery state, policy/version markers, and runtime
event replay so fresh sessions, reconnects, process restarts, Stop, and legacy
sessions remain predictable?
