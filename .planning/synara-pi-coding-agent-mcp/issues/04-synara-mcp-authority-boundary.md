# Synara MCP user-authorized operation boundary

Labels: `wayfinder:grilling`

## Question

How should “Synara MCP has all Synara permissions” be represented and enforced
so every operation available to the current user is reachable when enabled,
while project/thread ownership, approval, active-turn, Stop, cancellation,
credential rotation, audit, and cross-session boundaries cannot be bypassed?
