# Ticket 02 — Desktop managed real-Pi acceptance review

## Review state

Completed — **PASS, no findings**. This is the independent feature-level review required before Ticket 02 may enter its one final-acceptance consultation. It is evidence only; it does not accept Ticket 02.

## Candidate

- Range: `e65937228..c9c8278eb`
- `e50bff2ea` — Ticket 02 WP-C standalone desktop-managed real-Pi acceptance suite and wall-clock registration.
- `c9c8278eb` — remediation of the first independent review's two evidence findings.
- Pinned input: Alfie `aa6fa4a8540644d2509b10d6df854486ddc67d1d` / `@alfie/pi-subagents@0.15.0-alfie.4`.

## Independence and scope

The final reviewer was independent of the implementing worker. It reread the Ticket 02 authority, inspected the cumulative candidate and relevant production/harness paths, checked the pinned provenance, and reran the complete standalone acceptance file rather than relying only on the implementation report.

The cumulative candidate touches exactly the approved paths:

- `apps/server/src/provider/piSubagentDesktopManagedRealPiAcceptance.test.ts`
- `apps/server/vitest.config.ts` (one wall-clock suite registration)

No production, artifact-staging, verifier, gate, Alfie, dependency, or planning source changed in the candidate range. `git diff --check e65937228..c9c8278eb` was clean.

## Prior review findings and remediation

The first independent review of the WP-C candidate found two gaps. `c9c8278eb` resolves both without widening the approved write set:

1. **AC5 exact runtime-config detail.** The initial check allowed a containing string. The public fields are `Cause.pretty` envelopes, not the raw provider detail. The remediation verifies the fixed first-line envelope, extracts its provider-detail segment, and compares that segment by exact equality with `PI_SUBAGENT_DESKTOP_MANAGED_RUNTIME_CONFIG_FAILURE_DETAIL` on both the public turn-start failure and persisted session error. This preserves the distinct, bounded bootstrap details for malformed and unsupported bridge responses.
2. **AC4 post-detach liveness.** The initial check established only that progress and heartbeat existed. The remediation observes seq-2, proves pre-detach progress is absent, waits for seq-3 detachment, and requires both producer timestamps to be strictly after the detached event timestamp. In the controlled loopback, the child response is delayed 4 seconds while foreground attachment is 300 ms; timestamps are generated by the same extension process clock. The test continues to prove one seq-40 terminal result and post-terminal active-registry cleanup.

## Criterion evidence

| AC                                                  | Verdict | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --------------------------------------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AC1 — controlled artifact isolation                 | PASS    | The desktop-mode public-WebSocket harness stages and verifies the controlled release artifact, loads the managed Agent extension only from that artifact, and proves user/global and parent-directory decoys remain unused and bytewise unchanged.                                                                                                                                                                                                                                                      |
| AC2 — handshake before exposure and early failure   | PASS    | The successful path observes managed capability before admission. A corrupt artifact fails at the public boundary with bounded `digest_mismatch`, no managed admissions/capabilities/model requests, no fallback, and zero ledger effects measured after harness disposal.                                                                                                                                                                                                                              |
| AC3 — normal managed admission                      | PASS    | A supported public-WebSocket Agent request creates one durable non-rejected admission and lifecycle seq-1 accepted / seq-2 started records, with real Agent-bearing parent model traffic and matching card identity.                                                                                                                                                                                                                                                                                    |
| AC4 — detached lifecycle                            | PASS    | The slow real child crosses the foreground budget, records detached seq-3 inside the allowed timing envelope, emits persisted progress and heartbeat strictly after detachment, finishes with exactly one identity- and generation-fenced seq-40 terminal result, and clears active ownership only after terminal handoff.                                                                                                                                                                              |
| AC5 — fail-closed configuration and bridge failures | PASS    | Invalid selected user runtime config exposes exactly the runtime-config constant within both public `Cause.pretty` envelopes. Self-consistent hostile artifact copies drive real malformed and unsupported bridge handshake paths through the loader, extension, adapter, and public WebSocket boundary; each yields its distinct bounded bootstrap detail. All negative legs prove redaction, zero sessions/capabilities/admissions/cards/model requests, and zero durable ledger rows after disposal. |

## Fresh independent execution

The reviewer ran the candidate's complete standalone wall-clock suite with the clean pinned Alfie checkout:

```text
ALFIE_REPO_DIR=/Users/anhpham99/alfie \
  /Users/anhpham99/.bun/bin/bun run test \
  src/provider/piSubagentDesktopManagedRealPiAcceptance.test.ts --project=wallclock

1 file passed; 6 tests passed; 0 skipped
```

The observed AC4 run reported a 298 ms foreground attachment against the 300 ms budget, a null pre-detach progress baseline, and post-detach heartbeat/progress timestamps several seconds after the detached event.

The implementing candidate's focused regressions also passed before the independent review:

```text
PiAdapterDesktopManagedBootstrap
piSubagentArtifactClosureRealLoad
piSubagentArtifactVerifier
piSubagentDesktopArtifactGate

4 files passed; 95 tests passed
```

## Conclusion

The complete Ticket 02 candidate has independent PASS evidence for AC1–AC5 and may proceed to exactly one Supervisor final-acceptance consultation. Ticket 02 is **not accepted** by this review.
